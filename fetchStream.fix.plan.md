# 修复 fetchStream.ts 中的 ESLint 错误

## 任务描述
修复 app/src/util/fetchStream.ts 文件中第52-55行的ESLint错误：禁止嵌套If。请合并判断条件(&&)或提取函数。

## 问题分析
当前代码在第52-55行有一个嵌套的if语句：
```typescript
if (dataStr === "[DONE]") {
    ctx.onDone(); // 调用结束回调
    return; // 流结束
}
```

这个if语句位于for循环内部，ESLint规则要求避免嵌套的if语句，应该使用合并判断条件或提取函数的方式解决。

## 修复计划
1. 分析当前代码结构，确定最佳修复方案
2. 修改代码以符合ESLint规则
3. 确保修改后的代码功能完全一致
4. 验证修改没有引入其他问题

## 修复方案
将使用早期返回模式来避免嵌套if语句，这是最简洁且符合代码风格要求的解决方案。