# 思源笔记 Sync (数据同步) 辅助模块

`app/src/sync` 目录负责引导并管理思源笔记前端层面的数据云端同步逻辑。

## 目录结构与功能说明

- **[syncGuide.ts](file:///d:/dev/siyuan-note/app/src/sync/syncGuide.ts)**
  数据同步向导的实现。当用户初次开启同步或遇到同步冲突时，引导用户进行云端鉴权、方案选择及初始化全量同步流程。

---

## 注意事项
- 核心同步算法由 Go 内核（Kernel）实现，本目录主要负责与之配套的 UI 交互与异常处理提示。
