/**
 * 墓碑：本目录索引是合并后复活的旧系统流程聚合实现，不再承载运行时逻辑。
 *
 * - 内核故障界面：使用 `../../util/kernelFault` 的 `kernelError`。
 * - 同步重载、引用文本、标题、下载进度和锁屏：直达本目录对应职责模块。
 * - 退出、同步、进度和引用计数流程：使用 `../processSystem` 的现行实现。
 * - 文件树重建：使用 `../../util/file/rebuildDataIndex`。
 *
 * 保留本文件用于源码与 Git 历史查询；不重新导出替代实现，以便旧目录索引导入在编译期显式暴露。
 */
export {};
