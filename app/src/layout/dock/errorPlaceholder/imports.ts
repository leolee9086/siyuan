/** 用途：自描述序列化布局模型接口。使用范围：错误占位模型类型。解耦评估：纯类型契约，不加载布局 Model 具体实现。 */
import type {ILayoutSerializableModel} from "../../lifecycle/model.types";

/** 导出错误占位模型所需的最小序列化能力。 */
export type {ILayoutSerializableModel};
