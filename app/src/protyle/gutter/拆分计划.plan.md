拆分此文件夹下的gutter.ts,要求:
1.一次仅仅拆分一个工具函数
2.先完成拆分函数文件,并且保证其没有任何lint错误和ts类型错误之后,才能够将其应用到原始文件中
3.禁止一次同时进行多个部分的拆分和应用
4.拆分之前先检查是否存在尚未使用的拆分文件,如果存在,应该继续完善它直到能够应用到原始文件而不是继续新的拆分
5.仔细思考进行真正的功能拆分,而不是简单地将原始文件中的大片代码复制黏贴到新文件
6.每次开始拆分之前先确认本次执行的计划范围

已完成:
- [x] 拆分事件绑定逻辑到 `bindEvent.ts`

当前应该拆分:
- 考虑移除或完全拆分 `renderMenu` 和 `renderMultipleMenu` (涉及外部调用修改)
- 考虑移除或完全拆分 `render` (涉及外部调用修改)
拆分原则:
1.类中继续保留的逻辑应该和gutter所应该持有的状态高度相关
2.类主文件中尽可能保留数据操作而不是DOM操作