完成以下功能,有多个功能存在时请选择其一执行:

一 嵌入块菜单改进
1.嵌入块菜单应该参与转换为菜单的渲染
2.嵌入块菜单应该渲染一个转换当前搜索结果为块引用的项目
3.嵌入块菜单应该渲染一个移动当前搜索结果到其后方的项目
4.以上两个嵌入块菜单项目应该还包括一个转换完成之后删除嵌入块的版本

而 外观菜单改进

1. 添加一个字体菜单项目
2. 通过后端接口获取可用的字体
3. 每一个菜单项目应该能够直接地预览菜单项目


