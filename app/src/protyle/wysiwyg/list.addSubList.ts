/**
 * 墓碑：本文件曾维护一份与列表领域主实现分叉的 `addSubList`。
 *
 * - 唯一实现位于 `./list`，由 Alt+Enter 和列表行为测试直接使用。
 * - 旧实现手工拼装了不同的列表 HTML、事务和焦点行为，已不再作为生产入口。
 * - 后续列表领域拆分时，应从 `./list` 移动该唯一实现，不从本路径恢复或转发。
 *
 * 保留本文件用于源码与 Git 历史查询；不提供兼容重导出，使残留消费者成为明确诊断。
 */
export {};
