/**
 * 墓碑：本文件是合并后复活的旧通用 Tree 具体类，不再承载运行时逻辑。
 *
 * - Tree 的完整公共领域抽象：使用 `./tree.types` 的 `TreeDomain` 与 `TreeOptions`。
 * - 具体 Tree 的创建仅应出现在应用装配或领域组合边界；普通消费者依赖完整领域抽象。
 * - 仍直接构造旧 `Tree` class 的调用点必须迁入明确的创建边界，编译诊断用于完整列出这些位置。
 *
 * 保留本文件用于源码与 Git 历史查询；不伪造替代构造器或碎片接口，也不重新导出类型。
 */
export {};
