/**
 * 墓碑：本文件是合并后复活的旧网络请求实现，不再承载运行时逻辑。
 *
 * - `fetchPost`、`fetchSyncPost`、`fetchGet`：使用 `./network/fetch`。
 * - 请求头规范化与响应解码：使用 `./network/http/` 下的唯一实现。
 * - 消息处理、外部数据守卫和公共类型：使用 `./network/` 下对应职责模块。
 *
 * 上游 v3.8.0 对旧实现的全部行为修改已语义移植到现行所有者：
 * 竞态控制列表收缩（上游 15449）与 `fetchSyncPost` 的消息处理开关参数均落在 `./network/fetch`。
 *
 * 保留本文件用于源码与 Git 历史查询；不保留兼容包装或重导出，使旧网络入口消费者在编译期显式暴露。
 */
export {};
