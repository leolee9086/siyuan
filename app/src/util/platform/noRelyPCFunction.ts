/**
 * 墓碑：本文件是合并后复活的旧平台杂项实现，不再承载运行时逻辑。
 *
 * - 标签列表与重命名应归入标签领域，而不是平台工具层。
 * - `setLocalShorthandCount` 随移动端快捷方式改由内核直接提供而退役（上游 `38be2cd88`），不再存在前端调用点。
 * - `genTagList` 与 `renameTag` 的现行所有者为 `../../menus/tag.actions`。
 *
 * 保留本文件用于源码与 Git 历史查询；不创建新的杂项入口、兼容包装或碎片 Port。
 */
export {};
