/**
 * 墓碑：本文件是合并后复活的旧路径、笔记本和文件移动综合实现，不再承载运行时逻辑。
 *
 * - 文件路径、宿主跳转和移动基础操作：使用 `./file/pathName`。
 * - 纯路径计算：使用 `./file/path/operations`。
 * - URI 协议解析：使用 `./uri/protocol`。
 * - 笔记本状态及加密请求上下文：使用 `./file/notebook/store`。
 * - 文件移动对话框：使用 `./file/movePath/movePathTo`。
 *
 * 保留本文件用于源码与 Git 历史查询；不重新导出替代实现，避免继续维持综合路径入口。
 */
export {};
