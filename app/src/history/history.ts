/**
 * 墓碑：本文件是合并后复活的旧历史面板聚合实现，不再承载运行时逻辑。
 *
 * - 文档历史：使用 `./doc`。
 * - 差异查看：使用 `./diff`。
 * - 文档与仓库事件：使用 `./history.docEvent` 与 `./history.repoEvent`。
 * - 历史内容渲染：使用 `./history.render`。
 * - 键盘与侧栏尺寸：使用 `./keydown` 与 `./resizeSide`。
 * - 尚指向旧 `openHistory` 聚合入口的消费者必须迁入明确的历史面板组合边界。
 *
 * 保留本文件用于源码与 Git 历史查询；不提供兼容重导出，使未完成迁移的消费者成为明确诊断。
 */
export {};
