/**
 * 设置面板位置参数
 * 用途：定义设置面板在显示时的位置参数约束
 * 使用场景：设置面板的定位计算
 */
export interface 设置面板位置参数 {
    element: HTMLElement;
    targetElement?: HTMLElement | undefined;
    x?: number | undefined;
    y?: number | undefined;
}
