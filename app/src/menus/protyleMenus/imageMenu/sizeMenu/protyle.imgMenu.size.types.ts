/**
 * 作用：描述宽高菜单输入框与滑杆共享的上下文状态。
 * 意图：将“当前滑杆输入框引用”提取成独立类型，避免在业务文件中直接定义 type。
 * 调用时机：宽度和高度菜单子模块创建并绑定事件时。
 * 问题/改进：当前仅存储一个 rangeElement，后续可扩展更多共享状态字段。
 */
export type 图片尺寸菜单上下文 = {
    rangeElement: HTMLInputElement | null;
};
