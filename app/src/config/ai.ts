/**
 * 墓碑：本文件原有的 AI 设置对象已由声明式设置 Tab 完整承接，不再承载运行时逻辑。
 *
 * - 设置项注册：使用 `./tabs/aiTab` 的 `registerAiTab`。
 * - 复合界面与交互：使用 `./tabs/aiUi`。
 * - 配置读取与持久化：使用 `./tabs/aiRuntime` 的 `aiConfigApi`。
 *
 * 保留本文件用于源码与 Git 历史查询；不重新导出旧 `ai` 对象，以便旧导入在编译期显式暴露。
 */
export {};
