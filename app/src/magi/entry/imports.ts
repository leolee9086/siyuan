/** 用途：Vue 响应式类型（ComputedRef、InjectionKey、Ref）。使用范围：MAGI 入口模块类型依赖。解耦评估：第三方库类型导入，通过 imports.ts 收口。 */
import type { ComputedRef, InjectionKey, Ref } from "vue";
export type {ComputedRef};
export type {InjectionKey};
export type {Ref};