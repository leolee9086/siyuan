## 导入和导入

一个ts文件,除非文件名以.utils.ts或index.tx结尾否则只能有唯一的一个具名导出.

如果现有文件中发现多个导出的ts文件,应该检查哪些导出是需要的,并且进行修正

任何ts文件不能够跨越文件夹导入其它文件夹的内容,除非它的命名是imports.ts

任何依赖库不得在imports进行转发导入,应该以deps.ts进行转发导出

同一个文件中,如果使用了ctx参数,那么最多只能使用一种类型的ctx

