# Embedding 模块开发计划

## 下一步计划 (2025-12-24)

### 1. 前端数据集管理 Dock
- **块数据集**：管理已嵌入的内容块,使用前端或者后端ollama对接进行自动块嵌入
- **Assets 数据集**：管理图片、PDF等素材的嵌入
- **自定义数据集**：用户自定义的向量集合

### 2. Ollama 状态监控
- 数据集面板需要实时显示 Ollama 连接状态
- 连接断开时提供重连/配置入口

### 3. 自然语言支持
- 嵌入块时支持自然语言描述（而不只是块ID）
- 搜索时支持自然语言查询
