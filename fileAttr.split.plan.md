# 文件属性功能拆分计划

## 目标
拆分 `app/src/menus/commonMenuItem.fileAttr.ts` 中的 `openFileAttr` 函数，该函数过长（约300行），违反了函数规则。

## 分析
`openFileAttr` 函数包含以下主要功能：
1. 初始化变量和查找 protyle
2. 处理属性数据生成 HTML
3. 创建对话框
4. 设置初始值
5. 绑定复杂的事件处理器（包括多个子功能）
6. 绑定输入框事件

## 拆分策略
按照重构策略-拆分.md 的最小修改原则，逐步拆分：

### 第一步：拆分属性处理逻辑
- 创建 `processAttributes` 函数，处理属性数据并生成 HTML
- 提取第52-74行的属性处理逻辑

### 第二步：拆分对话框创建逻辑
- 创建 `createAttrDialog` 函数，负责创建对话框
- 提取第76-146行的对话框创建逻辑

### 第三步：拆分事件处理逻辑
- 创建 `handleDialogEvents` 函数，处理对话框事件
- 提取第160-314行的事件处理逻辑

### 第四步：拆分子事件处理函数
- 创建 `handleTabSwitch` 函数，处理标签页切换
- 创建 `handleRemoveAttr` 函数，处理属性删除
- 创建 `handleBookmarkSelect` 函数，处理书签选择
- 创建 `handleAddCustomAttr` 函数，处理添加自定义属性

### 第五步：拆分输入框绑定逻辑
- 创建 `bindInputEvents` 函数，绑定输入框事件
- 提取第317-326行的输入框绑定逻辑

## 预期结果
拆分后的 `openFileAttr` 函数将变为一个协调函数，调用各个子函数完成功能，提高代码可读性和可维护性。

## 注意事项
1. 遵循函数规则，确保所有函数都是顶层函数
2. 保持参数显式传递，不使用闭包
3. 确保每个函数职责单一，高内聚低耦合
4. 保持原有功能不变，确保回归测试通过