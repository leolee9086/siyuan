# 自动水印任务

这是一个文学编程风格的定时任务脚本。它会监听指定文件夹，为新增的图片自动添加水印。

## 任务配置

首先定义任务的基本配置信息：

```go
package main

import (
	"siyuan/siyuan"
)

// 任务名称
var Name = "自动添加图片水印"

// 任务描述
var Description = "监听指定文件夹，为新增图片自动添加水印"

// 调度表达式（每5分钟执行一次）
var Schedule = "*/5 * * * *"

// 监听的目录路径（请修改为你的实际路径）
var WatchDir = "D:/Pictures/待处理"
```

## 核心逻辑

任务的主要执行函数。每次调度时会扫描目录并处理新图片：

```go
// Run 任务主函数
func Run(ctx *siyuan.定时任务上下文) error {
	siyuan.日志信息("开始执行水印任务: " + WatchDir)
	
	// 获取水印配置
	config := siyuan.获取图片水印配置()
	if config == nil || config.水印文本 == "" {
		siyuan.日志警告("水印配置为空，跳过执行")
		return nil
	}
	
	// 扫描目录中的图片
	files := scanImages(WatchDir)
	
	for _, file := range files {
		// 跳过已处理的文件
		if isProcessed(file) {
			continue
		}
		
		siyuan.日志信息("处理图片: " + file)
		
		// 生成输出路径
		output := generateOutputPath(file)
		
		// 添加水印
		err := siyuan.添加图片水印(file, output, config)
		if err != nil {
			siyuan.日志错误("添加水印失败: " + err.Error())
			continue
		}
		
		siyuan.日志信息("水印添加完成: " + output)
	}
	
	return nil
}
```

## 辅助函数

这些是任务使用的辅助函数：

```go
import (
	"os"
	"path/filepath"
	"strings"
)

// scanImages 扫描目录中的图片文件
func scanImages(dir string) []string {
	var files []string
	
	filepath.Walk(dir, func(path string, info os.FileInfo, err error) error {
		if err != nil {
			return nil
		}
		if info.IsDir() {
			return nil
		}
		
		ext := strings.ToLower(filepath.Ext(path))
		if ext == ".jpg" || ext == ".jpeg" || ext == ".png" || ext == ".gif" {
			files = append(files, path)
		}
		return nil
	})
	
	return files
}

// isProcessed 检查文件是否已处理
func isProcessed(path string) bool {
	return strings.Contains(path, "_watermarked")
}

// generateOutputPath 生成输出文件路径
func generateOutputPath(input string) string {
	ext := filepath.Ext(input)
	base := strings.TrimSuffix(input, ext)
	return base + "_watermarked" + ext
}
```

## 使用说明

1. 将此文档的 `ext-lang` 属性设为 `go`
2. 将 `ext-type` 属性设为 `cronjob`
3. 修改 `WatchDir` 为你要监听的实际目录
4. 在思源的导出设置中配置水印文本
5. 通过 API 或界面启用此任务

任务启用后会每5分钟检查一次目录，为所有未处理的图片添加水印。
