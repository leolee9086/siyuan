# 示例：每分钟创建文件

这是一个简单的定时任务示例，演示如何在思源笔记中创建一个每分钟执行一次的任务。

## 任务配置

```go
package main

import (
	"fmt"
	"os"
	"path/filepath"
	"time"
	"siyuan/siyuan"
)

// 任务名称
var Name = "每分钟创建文件"

// 任务描述
var Description = "每分钟在指定目录创建一个以当前时间命名的文件"

// Cron 表达式：每分钟执行一次
var Schedule = "*/1 * * * *"

// 目标目录：请修改为你实际存在的目录
var TargetDir = "d:/tmp/siyuan_cronjob_demo"
```

## 执行逻辑

```go
// Run 任务入口函数
// 上下文参数 ctx 可用于获取任务相关信息
func Run(ctx *siyuan.Context) error {
	siyuan.日志信息(fmt.Sprintf("任务开始执行，目标目录: %s", TargetDir))

	// 1. 确保目录存在
	if err := os.MkdirAll(TargetDir, 0755); err != nil {
		siyuan.日志错误("创建目录失败: " + err.Error())
		return err
	}

	// 2. 生成文件名（使用当前时间）
	now := time.Now()
	fileName := fmt.Sprintf("%s.txt", now.Format("2006-01-02_15-04-05"))
	filePath := filepath.Join(TargetDir, fileName)

	// 3. 准备文件内容
	content := fmt.Sprintf("这是由思源定时任务自动创建的文件。\n创建时间: %s\n任务名称: %s", 
		now.Format(time.RFC3339), Name)

	// 4. 写入文件
	if err := os.WriteFile(filePath, []byte(content), 0644); err != nil {
		siyuan.日志错误("写入文件失败: " + err.Error())
		return err
	}

	siyuan.日志信息("成功创建文件: " + filePath)
	return nil
}
```

## 使用方法

1.  将本文档的属性 `ext-lang` 设置为 `go`。
2.  将本文档的属性 `ext-type` 设置为 `cronjob`。
3.  点击所有的"编译"或"注册"按钮。
4.  修改代码中的 `TargetDir` 变量为你电脑上实际存在的路径。
5.  在定时任务面板中启用此任务。
