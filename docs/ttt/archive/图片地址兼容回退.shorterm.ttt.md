# 图片地址兼容回退

## 目标

对齐 `SACAssetsManager` 的受控原图/缩略图地址语义，兼容旧桌面 bundle
遗留的文件浏览 API 地址，避免图片页签把 API 请求落到静态构建目录并收到应用壳文本。

## 实现切片

- [x] 保留 `resolveAssetURL` 作为图片、音视频、下载入口的统一解析层。
- [x] 归一化 `api/s-forge/file-browser/...` 和
  `/stage/build/.../api/s-forge/file-browser/...` 两类旧地址。
- [x] 不改变普通相对资源、`file:`、UNC 和远程 URL 的既有语义。
- [x] 补充 URL 单元测试与真实 6806 页面图片加载证据。

## 证据

- `pnpm exec vitest --run test/asset/assetUrl.test.ts`：6 个用例通过。
- `pnpm exec vitest --run test/asset/AssetCard.interaction.test.ts test/sforge/fileBrowser/FileBrowserGalleryTab.interaction.test.ts`：2 个文件、9 个用例通过。
- `pnpm exec webpack --mode development --no-cache --env oneShot=true --env target=desktop`：桌面 bundle `main.70e6717c5fc0020ac6ac.js` 构建成功。
- 真实页面打开 `D:\dev\.artifacts\passive-income\guide-render\page-2.png`：
  `currentSrc` 为应用根 `/api/s-forge/file-browser/content/...`，响应为
  `200 image/png`，自然尺寸 `1241 × 1754`，失败占位数量为 `0`。
- `git diff --check` 通过。

## 后续构建复核（2026-08-08）

- `pnpm run dev:once` 全量目标构建通过，当前桌面 bundle 为 `main.07cc2d07da5cdc9033de.js`。
- 重新加载 `http://127.0.0.1:6806/stage/build/desktop/` 并打开 `page-2.png` 后，
  `img.currentSrc` 为 `http://127.0.0.1:6806/api/s-forge/file-browser/content/root-ebc8c460379294ef/.artifacts/passive-income/guide-render/page-2.png`，
  `complete=true`、自然尺寸 `1241 × 1754`，现场截图已正常显示。
- `pnpm exec vitest --run test/asset/assetUrl.test.ts test/asset/AssetCard.interaction.test.ts test/sforge/fileBrowser/FileBrowser.open.test.ts test/sforge/fileBrowser/FileBrowserGalleryTab.interaction.test.ts test/sforge/fileBrowser/FilePropertiesPanel.interaction.test.ts`：5 个文件、19 个用例通过。

## 残余风险

- 旧浏览器缓存仍可能保留旧 bundle，需要强制重载后观察最新资源。
- 真实 D 盘全量媒体验收属于 M5，不在本切片范围内。

## 状态

已完成，移入 `docs/ttt/archive/` 保留审计记录。
