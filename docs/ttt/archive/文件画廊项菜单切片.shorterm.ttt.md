# 文件画廊项菜单切片（TikTocTak）

> **归属**：[`文件浏览Dock与Monaco编辑器.ttt.md`](../文件浏览Dock与Monaco编辑器.ttt.md)
> **状态**：已完成，归档于 2026-08-09
> **范围**：为资源瀑布流卡片和表格行补齐参考项目的核心文件项上下文菜单；不宣称 SACAssetsManager 的批处理、Efu、颜色分析和文件移动菜单已全部迁移。

## 参考契约

- `SACAssetsManager/source/UI/siyuanCommon/menus/galleryItem.js` 的通用入口包含打开来源笔记、默认应用、文件管理器、所在目录新页签、文件地址/链接/缩略图复制和文件操作分组。
- S-Forge 复用全局 `Menu`、剪贴板兼容层、`openBy`、属性 Dock 查询和已有文件删除仓储；菜单层不直接读取或写入文件。
- 根地址只在桌面系统动作和复制动作边界拼成绝对路径，查询、删除和属性选择仍使用 `{rootID, path}`。

## 完成项

- [x] 新增 `FileBrowserGalleryMenu.ts`，卡片和表格行共用同一菜单动作映射。
- [x] 接入打开预览、绑定块所在笔记、默认应用、资源管理器定位、所在目录新页签、复制绝对地址、Markdown 文件链接、缩略图 Markdown、所在目录地址、属性 Dock 和删除确认。
- [x] 画廊页签按直接根与挂载根解析资源根；根缺失或不可写时保留显式页签错误，不生成替代地址。
- [x] 删除成功后清理共享选择、修正已加载/总数/分页游标和选中状态；删除标签明确对应当前后端永久删除契约，没有伪称回收站。

## 证据

- `pnpm exec vitest --run test/sforge/fileBrowser/FileBrowserGalleryMenu.test.ts test/sforge/fileBrowser/FileBrowserGalleryTab.interaction.test.ts test/sforge/fileBrowser/FileBrowserGalleryTableRow.interaction.test.ts`：3 个文件、13 个用例通过。
- `pnpm exec vitest --run test/sforge/fileBrowser`：31 个文件、103 个用例通过。
- `go test ./filebrowser ./filequery ./fileproperties`：通过。
- `pnpm run typecheck:protyle-contract`：通过；全量 `pnpm run typecheck` 仍受仓库既有诊断阻断，本切片文件无新增匹配诊断。
- `git diff --check`：通过。

## 未完成边界

- SACAssetsManager 的 Efu 视图、插件菜单、颜色分析、批量压缩/去重、移动/硬链接/软链接和上传入口仍待独立领域切片；当前菜单不以空动作或占位项宣称完成。
- 真实桌面窗口的系统 Shell 动作、根挂载恢复和删除后的后端索引刷新仍需 M1/M2 现场验收。
