# 图片显示错误复核（TikTocTak）

## 目标

复核用户截图中的图片破图问题，确保文件浏览器图片入口与 `SACAssetsManager@94c8534a` 的本地资源语义一致：缩略图和原图都走可授权、可编码、可诊断的 URL，不能把 API 路径拼入应用静态 bundle 基路径。

## 参考证据

- `D:\dev\SACAssetsManager\source\server\endPoints.js`：本地资源使用 `thumbnail/?localPath=...` 与 `raw/?localPath=...`，由服务端处理路径编码和实际 MIME。
- `D:\dev\SACAssetsManager\source\UI\components\assetGalleryPanel.vue`：本地路径存在时先读取扩展名，再由缩略图中间件生成图片入口。
- S-Forge：`app/src/asset/assetUrl.ts`、`app/src/asset/index.ts`、`app/src/sforge/fileBrowser/FileBrowserPreviewPanel.vue`、`app/src/asset/components/AssetCard.vue`、`kernel/api/file_browser.go`。

## 验收

- [x] `baseURL` 为 `/stage/build/desktop/` 或完整绝对构建路径时，根相对 `/api/s-forge/file-browser/...` 仍解析到应用 origin 根，不进入静态壳。
- [x] Windows 盘符、UNC、中文、空格、嵌套目录路径编码后可被浏览器直接使用（URL 边界测试与 Go 路径/MIME fixture 已覆盖）。
- [x] 原图响应保留真实 MIME 和字节；缩略图响应按实际字节识别 MIME（Go API/thumbnail 专项通过）。
- [x] Asset 页签、文件预览、AssetCard、表格行、属性 Dock 的最终图片 `src` 均经过同一解析入口；画廊缩略图在生成处也先解析为应用根 URL。
- [x] 图片请求失败后使用透明像素和图标占位，卡片高度不跳变（组件专项通过）。
- [x] `pnpm exec vitest --run` 图片专项、`go test` 文件浏览/缩略图专项、`pnpm run dev:once` 和真实运行页签强制刷新复核通过。

## 记录

| 时间 | 证据 | 结果 |
| --- | --- | --- |
| 2026-08-08 | 用户截图：`page-2.png` 请求为 `200 / text/plain / 440 B` | 根据旧 bundle 的拼接逻辑与响应大小确认是静态壳，不是图片字节 |
| 2026-08-08 | `pnpm exec vitest --run test/asset/assetUrl.test.ts test/asset/AssetCard.interaction.test.ts test/sforge/fileBrowser/FileBrowserGalleryTab.interaction.test.ts` | 3 个文件、14 个用例通过；根 API 绝对 `baseURL` 回归通过 |
| 2026-08-08 | `pnpm exec vitest --run test/sforge/fileBrowser` | 文件浏览相关 22 个文件、68 个用例通过 |
| 2026-08-08 | `go test ./thumbnail ./api ./filebrowser ./filequery -count=1` | 4 个 Go 包通过，原图/缩略图真实 MIME 与字节回归通过 |
| 2026-08-08 | `pnpm run dev:once`；`pnpm exec webpack --mode development --no-cache --env oneShot=true --env target=desktop` | 全量构建与桌面目标构建通过；最新桌面 bundle 为 `main.0404dff157c3880bb6d8.js`，包含 `getApplicationOrigin`，不含旧 `baseURL + "/"` 拼接 |
| 2026-08-08 | `pnpm run lint:file -- src/asset/assetUrl.ts`、`pnpm run typecheck:protyle-contract`、`git diff --check` | 新增 URL 解析无 lint 错误，类型检查和空白检查通过；`FileBrowserGalleryTab.vue` 的 561 行 P0 行数告警为存量问题 |
| 2026-08-08 | 当前 `http://127.0.0.1:6806/stage/build/desktop/` 展开 `D:\\dev\\.artifacts\\passive-income\\guide-render` 后双击 `page-2.png` | `img.currentSrc` 为 `/api/s-forge/file-browser/content/root-ebc8c460379294ef/.artifacts/passive-income/guide-render/page-2.png`，`complete=true`，`naturalWidth=1241`，`naturalHeight=1754`；不再命中 `text/plain` 应用壳 |
| 2026-08-08 | 同一运行页面的画廊缩略图 | 缩略图请求为 `/api/s-forge/file-browser/thumbnail?rootID=root-ebc8c460379294ef&path=.artifacts%2Fpassive-income%2Fguide-render%2Fpage-1.png&size=360`，图片实际加载，证明缩略图与原图分别走受控入口 |

## 后续构建复核（2026-08-08）

- 本轮 `pnpm run dev:once` 后桌面 bundle 更新为 `main.07cc2d07da5cdc9033de.js`。
- 重新加载运行页签并打开截图中的 `page-2.png`，图片请求仍落在应用根
  `/api/s-forge/file-browser/content/root-ebc8c460379294ef/.artifacts/passive-income/guide-render/page-2.png`，
  `complete=true`、自然尺寸 `1241 × 1754`，视觉上不再显示破图标。
- 组合回归：5 个前端测试文件、19 个用例通过；`pnpm run dev:once` 和 `git diff --check` 通过。
