# 前端架构渐进式迁移执行跟踪 (TikTocTak)

> **目标**: 为 S-forge 建立去中心化的前端架构,将散落在 `config/` 目录的虚拟插件迁移至标准 `internal-plugins` 目录,实现内核复用和彻底解耦。量化指标:完成 FileTree、Image、Thumbnail 三个核心插件的迁移,通过 strict-import lint 规则验证。
>
> **流程**: 这是一个滚动更新的执行路线图。
> 1. 从"近期计划"中认领一个任务。
> 2. 完成开发和测试。
> 3. 将其移动到"已归档/已完成"区域。
> 4. 将"中期计划"中的条目提升到"近期计划"。

---

## 🎯 核心原则

### S-forge 核心理念

1. **Backend as a Service (BaaS), Frontend as a Platform**
2. **Asset-Centric (资产中心主义)**: 附件（Assets）与笔记（Notes）具有同等重要性
3. **Pragmatic Native Integration (务实的原生集成)**: 优先调用 OS 原生能力

### 架构定义

#### 前端核心层 (Frontend Core Layer)
核心层仅包含应用的基础设施和生命周期管理,**不应包含**具体的业务功能实现。

核心层主要职责:
- **Infrastructure**: Layout, Dock, Menus, Dialogs, Kernel API 通信
- **Initialization**: `window.siyuan` 全局对象初始化, 语言环境加载
- **Plugin System**: 插件加载、生命周期管理、API 暴露

#### 插件层 (Plugin Layer)
所有具体的业务功能模块应尽可能作为插件实现:
- **Internal Plugins**: FileTree, Image/Assets, Thumbnail
- **Future Goals**: 将 Protyle 编辑器彻底解耦为独立插件
- **Community Plugins**: 第三方扩展

### 接口隔离策略 (Island Imports)

参考 [`toread/0_lints/strict-import.ts`](../../toread/0_lints/strict-import.ts) 规则,强制所有核心组件必须:
1. **禁止越级访问**: 禁止直接 `import ../` 访问父级或兄弟模块
2. **单一出入口**: 所有对外依赖必须通过本地 `imports.ts` 显式转发
3. **禁止隐式依赖**: 禁止直接导入第三方库,必须通过 Shell 层注入或统一接口层获取

### 依赖分析与解耦方法论

遵循 **"显式化 -> 分析 -> 解耦"** 的严格流程:
1. **集中 (Centralize)**: 创建本地 `imports.ts`,将所有对外部的依赖收敛于此
2. **分析 (Analyze)**: 通过检查 `imports.ts`,识别不合理的依赖
3. **解耦 (Decouple)**: 使用标准 Plugin API 替代硬编码逻辑

### 规约完成标志

规约步骤被视为完成的**唯一**客观标准:
1. **零报错**: 目标模块在启用 `strict-import` 规则下,无任何 Lint 报错
2. **零豁免**: 代码中**不包含**任何 `// eslint-disable` 或 `// @ts-ignore` 用于绕过导入规则的注释
3. **全收敛**: 该模块目录下存在 `imports.ts`,且模块内所有对外引用均来自此文件

### 验证检查清单

每个阶段完成后必须验证:
- [ ] 启动 App,检查控制台无报错
- [ ] 检查设置 -> 插件管理或直接检查功能
- [ ] 验证对应功能面板可正常打开和使用
- [ ] 通过 strict-import lint 规则验证(如适用)

---

## ℹ️ 如何维护此文档

1. **完成归档**: 任务完成后,**必须**剪切粘贴到【已归档】列表,并打上 `[x]` 和日期。
2. **补充弹药**: 当【近期计划】空了,从【中期计划】里挑选任务挪上去。
3. **因地制宜**: 如果发现计划不合理,随时修改或删除。
4. **数据驱动**: 用数据说话,不凭感觉。

---

## 🟢 近期计划 

- [ ] **Phase 1: 建立插件目录结构 (P0)**
  - **背景**: 需要创建标准的 internal-plugins 目录结构,为后续插件迁移做准备
  - **行动**:
    1. 创建 [`app/src/internal-plugins/`](../../app/src/internal-plugins/) 目录
    2. 创建子目录: `file-tree/`, `image/`, `thumbnail/`
    3. 每个子目录创建 `index.ts` 入口文件
  - **验收标准**:
    - 目录结构创建完成
    - 每个插件目录包含 `index.ts` 文件
  - **参考文档**: 本文档"目录结构"章节

- [ ] **Phase 2: FileTree 插件迁移 (P0)**
  - **背景**: FileTree 是核心功能,需要从 config/fileTree.ts 的隐式注册变为显式插件类
  - **行动**:
    1. 在 [`app/src/internal-plugins/file-tree/index.ts`](../../app/src/internal-plugins/file-tree/index.ts) 创建 `FileTreePlugin` 类
    2. 继承 `Plugin` 类,实现 `onload()` 方法
    3. 使用 `addTab()` 注册文档树页签
    4. 清理 [`app/src/config/fileTree.ts`](../../app/src/config/fileTree.ts) 中的副作用代码
    5. 保留配置数据供插件使用
  - **验收标准**:
    - FileTreePlugin 类创建完成
    - 文档树功能正常工作
    - config/fileTree.ts 无副作用代码
    - 通过验证检查清单
  - **参考文档**: 本文档"第二阶段:FileTree 插件迁移"章节

- [ ] **Phase 3: Image 插件迁移 (P0)**
  - **背景**: Image 插件依赖手动 DOM 操作,需要迁移到插件类的方法中
  - **行动**:
    1. 在 [`app/src/internal-plugins/image/index.ts`](../../app/src/internal-plugins/image/index.ts) 创建 `ImagePlugin` 类
    2. 注册主资源页签和未引用/丢失资源页签
    3. 复用 [`app/src/config/image.ts`](../../app/src/config/image.ts) 中的辅助函数
    4. 清理 config/image.ts 中的副作用代码
  - **验收标准**:
    - ImagePlugin 类创建完成
    - 资源管理功能正常工作
    - config/image.ts 变为纯函数库
    - 通过验证检查清单
  - **参考文档**: 本文档"第三阶段:Image 插件迁移"章节

---

## 🟡 中期计划 

- [ ] **Phase 4: 核心加载策略实现 (P1)**
  - **背景**: 实现显式静态初始化策略,明确核心插件的加载顺序
  - **行动**:
    1. 修改 [`app/src/index.ts`](../../app/src/index.ts) 的 App 类
    2. 在 `initCore()` 方法中显式初始化各插件
    3. 按顺序加载: ThumbnailPlugin -> FileTreePlugin -> ImagePlugin
    4. 保持社区插件的动态加载机制
  - **验收标准**:
    - 核心插件按正确顺序加载
    - 应用启动无报错
    - 所有功能正常工作

- [ ] **Phase 5: Thumbnail 插件实现 (P1)**
  - **背景**: 实现 Asset-Centric 核心能力,为 FileTree 和 Image 提供缩略图服务
  - **行动**:
    1. 创建 [`app/src/internal-plugins/thumbnail/index.ts`](../../app/src/internal-plugins/thumbnail/index.ts)
    2. 实现 `ThumbnailPlugin` 类
    3. 提供缩略图生成和管理 API
  - **验收标准**:
    - ThumbnailPlugin 创建完成
    - 提供可用的缩略图 API
    - 其他插件可正常调用

- [ ] **Phase 6: 接口隔离规则实施 (P1)**
  - **背景**: 在代码物理层面强制实现解耦,采用孤岛导入策略
  - **行动**:
    1. 为每个插件创建 `imports.ts` 文件
    2. 收敛所有对外部的依赖到 `imports.ts`
    3. 启用 strict-import lint 规则
    4. 修复所有 lint 报错
  - **验收标准**:
    - 每个插件目录包含 `imports.ts`
    - 通过 strict-import lint 规则
    - 无任何 eslint-disable 或 ts-ignore 绕过

- [ ] **Phase 7: 配置文件彻底清理 (P2)**
  - **背景**: 将 config/image.ts 等文件的视图逻辑完全移除,只保留纯配置
  - **行动**:
    1. 将 genHTML 等视图逻辑移到插件类或 Vue 组件
    2. config 文件只保留配置数据
    3. 更新所有引用
  - **验收标准**:
    - config 文件无视图逻辑
    - 功能正常工作
    - 代码结构更清晰

---

## 🔴 远期计划 

- [ ] **Phase 8: Protyle 编辑器解耦 (P2)**
  - **愿景**: 将 Protyle 编辑器彻底解耦为独立插件,使其能够脱离核心层的 Tab/Layout 系统,在任意支持前端技术栈的环境中复用

- [ ] **Phase 9: 后端原生缩略图生成 (P2)**
  - **愿景**: 移除对外部 C# 进程的依赖,实现单二进制文件分发
  - **技术方案**:
    - Windows: 使用 Go syscall/cgo 调用 IShellItemImageFactory (COM 接口)
    - macOS: 使用 cgo 调用 QuickLook 框架
    - Linux: 遵循 freedesktop.org 规范

- [ ] **Phase 10: 社区插件生态建设 (P2)**
  - **愿景**: 建立完善的社区插件开发文档和示例,吸引第三方开发者

---

## 🏁 已归档/已完成

(暂无已完成任务)

---

## 📊 进度跟踪

- **总体进度**: 0/10 (0%)
- **近期计划**: 0/3 (0%)
- **中期计划**: 0/4 (0%)
- **远期计划**: 0/3 (0%)

---

## 📚 技术详情参考

### 目录结构

```text
app/src/
├── internal-plugins/           # [NEW] 存放所有内置插件
│   ├── file-tree/              # [NEW] 文档树插件
│   │   └── index.ts
│   ├── image/                  # [NEW] 图片/资源管理插件
│   │   └── index.ts
│   └── thumbnail/              # [NEW] 缩略图生成/管理插件 (Asset-Centric Core)
│       └── index.ts
├── config/
│   ├── fileTree.ts             # [MODIFY] 仅保留配置数据,移除插件注册逻辑
│   └── image.ts                # [MODIFY] 仅保留配置数据,移除插件注册逻辑
└── index.ts                    # [MODIFY] App 类加载 internal-plugins
```

### FileTree 插件实现示例

```typescript
import { Plugin } from "../../plugin";
import { Custom } from "../../layout/dock/Custom";
import fileTreeConfigPanel from "../../components/panels/fileTreeConfig.panel.vue";
import { createApp } from "vue";
import { fileTree } from "../../config/fileTree";

export class FileTreePlugin extends Plugin {
    static displayName = "文档树内部插件";
    static name = "internal-plugin-filetree";

    constructor(options) {
         super({ ...options, name: FileTreePlugin.name, displayName: FileTreePlugin.displayName });
    }

    onload() {
        this.addTab({
            type: "internal-filetree",
            init: (model: Custom) => {
                const tab = model.tab;
                const app = createApp(fileTreeConfigPanel);
                if (tab) {
                    app.mount(tab.panelElement);
                }
            }
        });
    }
}
```

### Image 插件实现示例

```typescript
import { Plugin } from "../../plugin";
import { image } from "../../config/image";

export class ImagePlugin extends Plugin {
    static displayName = "资源管理内部插件";
    static name = "internal-plugin-image";

    constructor(options) {
         super({ ...options, name: ImagePlugin.name, displayName: ImagePlugin.displayName });
    }

    onload() {
        // 注册主资源页签
        this.addTab({
            type: "internal-image",
            init: (model: Custom) => {
                const tab = model.tab;
                if (tab) {
                    tab.panelElement.innerHTML = image.genHTML();
                    image.bindEvent(tab.panelElement);
                }
            }
        });
        // 注册未引用/丢失资源页签... (逻辑同上)
    }
}
```

### 核心加载策略示例

```typescript
// app/src/index.ts
import { FileTreePlugin } from "./internal-plugins/file-tree";
import { ImagePlugin } from "./internal-plugins/image";
import { ThumbnailPlugin } from "./internal-plugins/thumbnail";

// ... Inside App class ...
private async initCore() {
    // 0. 显式初始化 Thumbnail Service (作为基础能力)
    this.thumbnailManager = new ThumbnailPlugin(this.options);
    await this.thumbnailManager.onload();

    // 1. 显式初始化 FileTree
    this.fileTree = new FileTreePlugin(this.options);
    await this.fileTree.onload();

    // 2. 显式初始化 Image Manager
    this.imageManager = new ImagePlugin(this.options);
    await this.imageManager.onload();

    // 3. 最后才加载不确定的第三方插件
    await this.loadCommunityPlugins();
}
```

---

## 📋 变更文件清单

### 新建文件
- [`app/src/internal-plugins/file-tree/index.ts`](../../app/src/internal-plugins/file-tree/index.ts)
- [`app/src/internal-plugins/image/index.ts`](../../app/src/internal-plugins/image/index.ts)
- [`app/src/internal-plugins/thumbnail/index.ts`](../../app/src/internal-plugins/thumbnail/index.ts)

### 修改文件
- [`app/src/index.ts`](../../app/src/index.ts) - App 类加载逻辑
- [`app/src/config/fileTree.ts`](../../app/src/config/fileTree.ts) - 移除副作用代码
- [`app/src/config/image.ts`](../../app/src/config/image.ts) - 移除副作用代码

---

**文档创建**: Roo (Code Mode)  
**最后更新**: 2026-01-26  
**版本**: 1.0.0
