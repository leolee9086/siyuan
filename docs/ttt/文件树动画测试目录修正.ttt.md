# 文件树动画测试目录修正

> 目标：将文件树动画测试从源码目录迁移到统一测试目录，并保持测试可执行。

- [-] 将测试迁移到 `app/test/layout/dock/` 并修正源码导入路径
- [ ] 执行单文件测试与前端 lint

## 执行记录

- 2026-07-26：确认 `app/test/` 是前端测试统一目录，`app/scripts/test-files.mjs` 同时收集 `src` 与 `test` 下的测试文件。
- 2026-07-26：首次移动命令失败，终端不支持 `Move-Item`，文件未移动；改用终端支持的 Windows `move` 命令继续。
