# AssetMasonryDialog.vue 样式迁移执行跟踪 (TikTocTak)

> **目标**: 修复 `app/src/asset/components/AssetMasonryDialog.vue:247-288` 的ESLint错误，将 `<style>` 块迁移为 Tailwind CSS 或外部 CSS 文件
>
> **流程**: 这是一个滚动更新的执行路线图。
> 1. 从"近期计划"中认领一个任务。
> 2. 完成开发和测试。
> 3. 将其移动到"已归档/已完成"区域。
> 4. 将"中期计划"中的条目提升到"近期计划"。

---

## 核心原则

1. **样式功能保持**: 迁移后的样式必须与原有样式视觉效果完全一致
2. **遵循项目规范**: 使用 Tailwind CSS 或外部 CSS 文件，禁止使用 Vue `<style>` 块
3. **BEM命名保持**: 保持原有的 BEM 命名规范 (`asset-masonry-dialog__*`)
4. **CSS变量兼容**: 保持对项目 CSS 变量的引用 (`var(--b3-theme-*)`)

---

## 验证检查清单

- [ ] ESLint规则 `vue-custom/no-vue-style-block` 检查通过
- [ ] 组件视觉效果与迁移前一致
- [ ] 所有样式类正确应用
- [ ] CSS变量引用正常工作

---

## ℹ️ 如何维护此文档

1. **完成归档**：任务完成后，**必须**剪切粘贴到【已归档】列表，并打上 `[x]` 和日期。
2. **补充弹药**：当【近期计划】空了，从【中期计划】里挑选任务挪上去。
3. **因地制宜**：如果发现计划不合理，随时修改或删除。
4. **数据驱动**：用数据说话，不凭感觉。

---

## 🟢 近期计划

- [ ] **Phase 1: 分析现有样式并选择迁移方案 (P0)**
  - **背景**: 文件第247-288行包含 `<style lang="scss" scoped>` 块，违反 `vue-custom/no-vue-style-block` 规则
  - **行动**:
    1. 分析现有样式结构（6个CSS类，约40行SCSS）
    2. 评估迁移方案：
       - 方案A: 迁移到外部 SCSS 文件 `AssetMasonryDialog.scss`
       - 方案B: 转换为 Tailwind CSS 类
    3. 根据项目现有实践选择最佳方案
  - **验收标准**: 
    - 迁移方案已确定
    - 方案与项目现有实践一致
  - **参考文档**: 
    - 项目其他Vue组件的样式处理方式

- [ ] **Phase 2: 执行样式迁移 (P0)**
  - **背景**: 根据Phase 1确定的方案执行迁移
  - **行动**:
    1. 创建外部样式文件（如选择方案A）或转换为Tailwind类（如选择方案B）
    2. 删除Vue文件中的 `<style>` 块
    3. 在组件中引入外部样式（如适用）
    4. 确保所有样式类正确应用
  - **验收标准**: 
    - `<style>` 块已移除
    - 样式正确应用到组件
    - ESLint检查通过
  - **参考文档**: 
    - `docs/规程/代码质量/lint错误修复.procedure.md`

---

## 🟡 中期计划 

- [ ] **Phase 3: 视觉验证 (P1)**
  - **背景**: 确保迁移后的样式与原有样式视觉效果一致
  - **行动**: 
    1. 在开发环境中打开资源管理器对话框
    2. 验证布局、间距、颜色等视觉效果
    3. 测试不同状态（加载中、空状态、有数据）
  - **验收标准**: 视觉效果与迁移前完全一致

---

## 🏁 已归档/已完成

(暂无)

---

## 附录：原始样式内容


---

**文档版本**: 1.0.0  
**创建时间**: 2026-02-04  
**维护者**: Roo (Code Mode)
