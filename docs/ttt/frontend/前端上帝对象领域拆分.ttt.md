# 前端上帝对象领域拆分（TikTocTak）

> **最终目标**：将阻碍前端单向依赖的上帝模块按稳定领域职责拆分，使具体 class、全局查询、宿主装配和纯算法不再通过中央聚合对象互相反向依赖，并为“前端循环依赖类型解耦”提供可持续的领域边界。
>
> **当前目标**：沿 Editor URI/打开、菜单、AI 与 Protyle AV 主环继续清除总网关和具体组合入口依赖。
>
> **下一步任务**：核对 `editor/processSiYuanUri.ts` 与 `util/uri.ts` 的语义差异，确定 URI 领域的稳定唯一入口；随后审计 AV Asset 反向加载通用菜单组合入口的职责。

---

## 关联任务

- 主任务：[前端循环依赖类型解耦](./前端循环依赖类型解耦.ttt.md)
- 本文只追踪必须通过领域拆分才能解除的上帝模块；普通局部反向依赖仍由主任务追踪。
- 每个拆分阶段必须同时更新两份 TTT 的 SCC、简单环、测试和暂存证据。

## 不变量

- 保留并扫描所有 `imports.ts`；通过重新划定领域网关暴露真实依赖，不删除或绕开网关。
- 不以移动文件、动态导入、字符串模块路径、lint 忽略或类型降级掩盖循环。
- 类型可以依赖稳定领域类型；稳定类型不得反向依赖具体实现 class。
- 优先提取领域根与完整能力契约，不按单个调用点堆积细碎接口。
- 不用 `unknown` 代替可参数化的应用、页签、编辑器或模型身份；`unknown` 仅用于真实外部输入校验。
- 相同运行逻辑保持唯一实现；宿主差异确实不相同时允许分开实现。
- 工厂只负责装配，不持有跨调用生命周期状态；此类状态由统一注册表集中管理并提供显式重置。
- 迁移前后保持事件顺序、错误传播、DOM 生命周期、会话状态和公开行为一致。
- 一个拆分阶段只有在相关测试通过且 Tarjan SCC 净下降时才归档；仅改变环路径名称不算完成。

## 识别标准

满足下列多项且在 SCC 中形成大量扇入/扇出的模块，登记为上帝对象或上帝模块：

- 同时承担领域状态、实例创建、全局环境访问、遍历、分类和 UI 副作用。
- 一个 `imports.ts` 聚合多个子域，并被该域的核心 class 反向消费。
- 具体 class 被用于本应由稳定身份、判别字段或参数能力表达的判断。
- 单个模块变化会显著改变 Madge 枚举路径，却不能缩小 Tarjan SCC。
- 调用方为获取一个能力必须依赖完整 `App`、`Protyle`、`Tab`、`Wnd` 或中央注册表。

## 现状基线

- **2026-07-24**：主任务初始 SCC 为 `1020,14,8,4,2,2,2,2`。
- **2026-07-25 当前批次前**：唯一 SCC 为 `922`，Madge 简单环为 `1147`。
- **2026-07-25 当前状态**：`lint:cycles` 已严格限定为 `app/src`，扫描 2,122 个源码文件并枚举 `1015` 条代表环；首条路径为 `editor/imports.ts -> layout/getAll.ts -> asset/index.ts -> components/PDFviewer.vue -> protyle/util/compatibility.ts -> editor/processSiYuanUri.ts`。
- `layout/getAll.ts` 同时包含布局遍历、十余种具体模型 `instanceof` 分类、全局环境读取和面板集合查询。
- `editor/index.ts` 是具体 Editor 模型，却通过 `editor/imports.ts` 依赖全域聚合；该网关反向包含 Layout、Protyle、Dialog、模型查询和 Editor 周边行为。
- `util/file/pathName.ts` 同时承担路径适配、显示名、文件树请求、DOM 动画和笔记本状态；纯消费者会被带入完整 UI 依赖。
- `plugin/index.ts` 的 Plugin class 同时持有插件领域状态和 loader/Dock 装配行为，loader 又必须依赖该 class 的运行时身份。
- 窗口 hash 与 Editor 模型已参数化，`Editor -> setHeader` 直接环归零，公共入口、模型实现和模型网关均已退出 SCC；布局 Model、BlockPanel 支撑子域和 Outline 树交互领域根也已退出，app 工厂仍在 SCC，当前图为单个 `874`。

## 目标架构

### Editor 模型域

- 具体 Editor class 位于独立模型子域，只依赖 Protyle 创建、配置读取、字数统计、最近文档写入和宿主回调。
- Editor 公共入口只公开模型身份，不聚合 Editor 全域工具。
- Editor 创建工厂绑定宿主能力；窗口 hash 同步作为必需参数，不使用可选 hook 或空实现。
- `instanceof Editor` 仅保留在确实需要具体实现身份的分类边界，普通消费者使用稳定能力契约。

### Layout 查询域

- 纯布局遍历只依赖 `Branch/Window/Tab` 结构泛型，保留真实身份且不导入具体 class。
- 模型分类单独拥有具体模型映射，不与窗口/页签遍历实现混合。
- 全局环境查询负责取得当前布局、移动端和对话框集合，再调用纯遍历或分类函数。
- `getAll.ts` 最终缩减为兼容入口或被明确领域入口替代，不继续拥有全部实现。

## 近期计划

- [ ] **Editor 模型子域**
  - [x] 提取 `IWindowHashModel` 与严格判别身份，Editor/Asset 通过 getter 暴露当前值。
  - [x] 用必需构造能力替代 Editor 对 `setHeader` 的动态导入，直接环数量归零。
  - [x] 将 Editor 具体实现迁出综合网关闭包，建立只覆盖模型构造依赖的子域 `imports.ts`。
  - [x] 更新五个创建入口统一使用参数化工厂，保证所有实例同步语义一致。
  - [x] 参数化应用、页签和编辑引擎身份；Editor 入口、模型实现和模型网关退出应用级 SCC。
  - [ ] 下沉 app 级 Protyle、全屏和窗口 hash 绑定，使两个 Editor 工厂模块退出 SCC。
- [ ] **Layout 查询域**
  - [x] 提取结构化布局遍历唯一实现，并新增嵌套窗口/页签顺序测试。
  - [ ] 为参与分类的具体模型建立完整公共领域根、模块级厂牌与独立双向契约校验，分类器仅依赖领域身份。
    - [x] Asset、Graph、Outline、Backlink、Files、Forwardlink、Bookmark、Custom、Tag 已完成；`IModels` 同步改用完整抽象类型。
    - [x] Search、Editor 已完成，禁止为分类单独创建局部接口。
  - [ ] 将模型分类从 `getAll.ts` 提取为独立具体实现边界。
  - [ ] 将全局环境读取与纯遍历组合迁入查询入口。
  - [ ] 逐步迁移调用方，移除中央模块对无关模型 class 的扇出。
- [ ] **后续候选审计**
  - [ ] `App`：按启动装配、布局宿主、插件宿主和全局事件能力评估。
  - [ ] `Protyle`：按编辑器状态、渲染、选区、工具栏和生命周期评估。
  - [ ] `Menu`/Dock 入口：按状态所有者、条目构建和宿主动作评估。
  - [x] `pathName`：path 与 notebook 两个稳定子域已迁出，文件树请求和 DOM 行为留待后续继续拆分。
  - [x] `Plugin`：loader 生命周期能力已通过必需运行时契约注入，class 不再反向导入 loader。

## 中期计划

- 为每个已确认上帝模块记录入边、出边、状态所有权和可独立测试的领域根。
- 迁移调用方时优先传递完整领域上下文，不创建逐函数参数袋或能力碎片。
- 将具体实现绑定集中在 factory/bootstrap 边界，并验证这些边界自身不进入反向闭环。
- 每完成一个领域，重新读取 `pnpm lint:cycles` 最短环和 Tarjan SCC，决定下一个拆分对象。

## 风险

- **行为漂移**：遍历顺序、未初始化页签、Electron 窗口判断和模型动态值必须保持原语义。
- **形式解耦**：把依赖移动到 factory 或新网关但 SCC 不下降时，不得归档为完成。
- **类型退化**：递归布局和宿主身份若用 `unknown` 或断言拼接，会隐藏真实契约错误。
- **并行修改**：工作树存在大量已暂存变更；只增量暂存本任务文件，不重置其他改动。

## 验收标准

- 对应上帝模块不再同时拥有多个独立领域的运行实现。
- 新领域根拥有直接测试，专项 lint 和 `git diff --check` 通过。
- 每个归档阶段的 Tarjan SCC 相比阶段基线净下降，且没有新增独立 SCC。
- `pnpm lint:cycles` 最终为零；全部 `imports.ts` 保留并参与扫描。
- Node、Vitest、浏览器契约测试及最终 TypeScript 检查全部通过。

## 已完成记录

- **2026-07-25**：建立本 TTT。窗口 hash 身份与纯布局遍历已有唯一实现和契约测试；Editor 模型子域不再依赖具体 App、Tab、Protyle 或全屏实现，公共入口、模型实现和模型网关退出 SCC。Node 测试 `127/127`，新增文件专项 lint 通过；全量类型检查在 8 GB 堆、240 秒内没有诊断但未完成。当前 app 工厂仍在 SCC，唯一组件为 `923`，因此近期阶段继续滚动而不归档。
- **2026-07-25**：`pathName` 首轮拆分完成：路径显示名/POSIX 适配和笔记本状态拥有独立单向子域，旧入口维持导出兼容；3 项路径测试通过。Plugin loader 的应用回边改由完整 `AppFacade.pluginHost` 绑定，Plugin 构造器和内部资源插件不再接收两方法 runtime 对象。相关纯契约/子域均在 SCC 外；继续按内部边界审计让 `getSavePath`、`fileHtmlGenerator`、`getAll.guard` 和 `ui-utils.environment` 退出组件，应用级 SCC 从 `923` 降至 `919`，简单环为 `1220`。下一步继续拆分 `getAll` 与 app Editor 工厂。
- **2026-07-25**：边界叶节点继续通过稳定领域依赖退出组件：拼音转换由专用网关持有；更新日志由宿主注入完整 Dialog Port，不再让 boot 网关依赖具体 Dialog class。唯一 SCC 从 `919` 降至 `916`，简单环仍为 `1220`，Node `130/130`。这两项不替代 Editor/Layout 上帝模块拆分，近期任务保持未完成。
- **2026-07-25**：识别并拆开 EventBus class 内混入的插件菜单 UI 编排，公开事件总线身份与生命周期保持不变，具体菜单工厂仍在组合层依赖完整 Plugin，未保留零碎参与者接口。ProfileManager 通过专用存储网关退出综合 config 依赖。唯一 SCC 从 `916` 降至 `901`，简单环降至 `1102`，Node `130/130`；Editor/Layout 近期任务继续滚动。
- **2026-07-25**：Tree class 直接复用 emoji 纯渲染实现并退出 SCC，唯一组件从 `901` 降至 `900`，简单环为 `1101`；这属于边界依赖校正，不新增上帝模块拆分项。
- **2026-07-25**：快捷键平台格式化从 Protyle compatibility 迁入稳定 hotkey 子域，释放 `Menu.uills/MenuItem` 及关联边界节点；唯一 SCC 从 `900` 降至 `895`，简单环为 `1100`，Node `130/130`。该批次保持唯一实现，不新增上帝模块拆分项。
- **2026-07-25**：快捷键领域继续收口 `isOnlyMeta/isNotCtrl` 唯一实现，Protyle 入口仅静态转发，相关节点退出后唯一 SCC 从 `895` 降至 `890`；该项仍属于稳定领域依赖校正。
- **2026-07-25**：稳定标题/图片基础设施直达将唯一 SCC 从 `890` 降至 `888`；`TabRegistry` 的领域状态与泛型创建协议和具体 Dock 模型装配分离，注册表及入口退出 SCC，组件进一步降至 `886`，简单环为 `1098`。注册表 4 项测试位于专门的 `app/test/registry`，完整 Node 回归 `134/134`；全量 TypeScript 在 8 GB 堆、300 秒内无诊断但未完成，继续保留为最终回归待办。
- **2026-07-25**：布局 `Model` class 不再把应用和父宿主固定为具体 `App/Tab`，改由完整领域身份参数化，并由 18 个具体模型在实现边界精确绑定；WebSocket 创建保持同域唯一工厂，连接状态仍由基类独占。高入边基类退出 SCC，唯一组件从 `886` 降至 `885`，简单环降至 `1081`。Model 专项 `11/11`、Node `134/134`、Vitest `465/465` 通过；全量 TypeScript 在 8 GB 堆、360 秒内无诊断但未完成，近期任务继续滚动。
- **2026-07-25**：移动平台判断与事件名选择迁入稳定 platform 领域并保持 compatibility 静态转发，四个工具栏/正向链接边界节点退出，唯一 SCC 从 `885` 降至 `881`，简单环为 `1076`；平台专项 `3/3`、Node `134/134`、Vitest `468/468` 通过。该项属于稳定领域依赖校正，不替代 Editor/Layout 上帝模块后续拆分。
- **2026-07-25**：BlockPanel 的 render/observer/editor 支撑职责不再共享包含具体 Protyle、App、布局和 AV 实现的综合网关。观察器动作与 AV 定位动作由根组合层通过既有上下文注入，子域分别拥有只含稳定外部依赖的 `imports.ts`，AV 请求类型与定位实现分离；三个支撑模块退出后唯一 SCC 从 `881` 降至 `878`，简单环保持 `1076`。新增生产文件 lint 清零，专项 `3/3`、Node `134/134`、Vitest `471/471` 通过；BlockPanel 根类的构造与 Dialog 生命周期仍在 SCC，后续继续拆分。
- **2026-07-25**：Outline 树筛选、高亮、层级展开和右键树动作统一依赖一个完整 `IOutlineTreePanel` 领域根，具体 class 负责实现并继续拥有状态，没有生成按调用点拆分的小接口。四个 helper 退出后唯一 SCC 从 `878` 降至 `874`，简单环降至 `1072`；Node `134/134`、Vitest `471/471` 通过。Outline class 的应用/页签编排和其余 helper 回边仍待拆分。
- **2026-07-25**：确认 AV `avRender`、table/gallery/kanban 渲染与后处理均为超规模组合函数，建立独立 [AV 渲染组合根与视图子域拆分](./AV渲染组合根与视图子域拆分.ttt.md) 追踪。首批将 Header 和根视图调度契约迁出，子视图通过参数回到根分派已解析数据，循环 `902 -> 900`；后续按数据获取、状态快照、具体 DOM 和后处理继续拆分，不以局部接口或更多实现反向导入维持上帝对象。
- **2026-07-25**：Layout 查询域首批完成：`getAll.ts` 复用纯遍历实现并依赖完整 `LayoutDomain/LayoutTab/LayoutWindow`，不再加载 `Layout/Tab/Wnd`；Asset 新建完整领域根，Graph/Outline/Backlink/Files/Forwardlink 复用既有完整领域根，六类模型以模块级 Symbol 厂牌保持同步名义分类。`PublicInstanceLooksLike` 契约位于 `app/test/layout/LayoutDomain.contract.test.ts`，同时修正 `LayoutDomain.resize` 在 `exactOptionalPropertyTypes` 下与具体类不等价的问题。代表环由 `1015` 阶段性变为 `1006`，首条路径推进到 Bookmark。
- **2026-07-26**：Outline 右键菜单继续按真实职责拆分：编辑菜单删除对剪贴板菜单的聚合转发，编辑器与标题块解析归入 `outline/editorContext`，edit/clipboard/contextMenu 共同直达该所有者并继续依赖完整 `OutlineDomain`。菜单两节点环归零；新上下文节点因 `getAllModels` 进入主 SCC，后续应处理布局模型查询边，不用菜单互转发或局部宿主接口隐藏。行为测试 `5/5`，代表环 `526 -> 525`，唯一 SCC阶段性 `682 -> 684`。
- **2026-07-26**：窗口位置交换工具与 Editor 页签守卫改用完整 `LayoutWindow/LayoutTab`，删除对具体 `Wnd/Tab` class 的类型依赖；`PublicInstanceLooksLike` 继续证明具体窗口完整覆盖领域根。Wnd/window-utils 直接环归零，守卫退出主组件，代表环 `525 -> 524`、最大 SCC `684 -> 683`；没有新增局部窗口契约或运行时分支。
- **2026-07-26**：Wnd 标签切换、添加、列表和超量清理 helper 改用完整 `LayoutWindow/LayoutTab`，应用身份由 class 组合根显式传入 `wndSwitchTab`；helper 不再读取 Wnd 私有 app，也不再依赖 Wnd/Tab class。Wnd/Wnd.tab 直接环归零，代表环 `524 -> 523`；tabAction 仍混合私有窗口销毁和构造生命周期，下一批先确定正式所有权，不用构造器断言或工厂闭包绕开。
- **2026-07-25**：Bookmark 建立完整领域根，直接复用 Tree 与 Protyle 完整抽象并保持私有状态封装；分类器和 `IModels` 改依赖 `BookmarkDomain` 厂牌。目标直接边归零，首条路径推进到 Custom；代表环阶段性变为 `1024`。
- **2026-07-25**：Custom 建立完整泛型领域根并用 `ThisType` 表达现有回调宿主语义；删除注册表碎片 `ICustomTabModel`，下层消费方统一改依赖 `CustomDomain`。具体 class 仅留在创建工厂和官方插件运行时身份边界，分类器直接边归零；首条路径推进到 Tag，代表环降至 `1009`，Node `145/145` 通过。
- **2026-07-25**：Tag 建立完整领域根，复用 Tree/Protyle 完整抽象；分类器与标签刷新调用方不再加载具体 class。该直接边归零后代表环从 `1009` 降至 `820`，Layout 查询域剩余具体模型集中在 Editor/Search，编辑器聚合仍返回具体 Protyle。
- **2026-07-25**：Search 与 Editor 均建立完整领域根、模块级 Symbol 厂牌和 `PublicInstanceLooksLike` 校验；`getAll.ts` 对 Editor/Search/Protyle 的具体实现边全部归零。布局实例查询与 PDF 加载门禁迁入各自子域并保留唯一实现，Editor 面板同步由布局组合根传入完整 `DockDomain`。当前图仍为唯一 SCC，大小 `782`；代表环 `863` 只记录路径枚举，阶段收益以具体实现边归零和职责方向修正为依据。
- **2026-07-25**：Search 默认类型投影建立独立 defaults 子域，只依赖配置环境；删除旧转发入口并让 Protyle compatibility、移动搜索和 Search UI 直达唯一实现。Search 存储写入统一复用稳定 storage 子域，不再加载 compatibility/AI 聚合网关。唯一 SCC 从 `782` 实质缩到 `748`，首环转移到 Emoji/Files 领域；代表环同步从 `863` 变为 `603`。
- **2026-07-25**：Files 的纯图标渲染不再加载 Emoji 面板组合入口，Emoji 更新文件树则依赖完整 `FilesDomain` 而非具体 class。代表环反升 `603 -> 632`，但唯一 SCC `748 -> 746` 且原首环消失；以 SCC 成员和依赖方向判定改造有效，继续按新的 Asset/PDF/Window/Dialog/Emoji 首环推进。
- **2026-07-26**：Layout 上帝模块中的递归序列化、布局快照、保存与导出编排已拆入真实职责所有者，旧聚合实现删除；保存重试状态不再由工厂闭包持有，统一登记在 SForge 状态注册表并具备显式生命周期重置。专项注册表测试 `2/2`、Symbol lint 规则测试 `4/4`、Node 全量回归 `154/154` 与 diff 校验通过；当前 `lint:cycles` 为 `2146` 个源码节点、`819` 条枚举环，唯一 SCC 从 `746` 缩到 `738`，该组合根拆分阶段归档。
- **2026-07-26**：Asset 预览与路径工具不再通过 `pathName`/Emoji 综合入口获取稳定纯实现，保持实现唯一且没有增加契约。枚举环 `819 -> 818`，唯一 SCC `738 -> 723`，证明 15 个模块退出组件；路径专项测试 `3/3` 通过。
- **2026-07-26**：无状态 Dock 查询从 `tabUtil` 上帝模块迁入 Layout query 子域，依赖完整 `LayoutRuntimeDomain` 与 `DockDomain`；具体 Layout/Dock class 继续由 `PublicInstanceLooksLike` 契约校验，没有新增局部能力接口。Emoji 退出 Tab 创建/模型构造链后，枚举环 `818 -> 719`，唯一 SCC `723 -> 708`；查询专项 `2/2`、Node `156/156` 通过。
- **2026-07-26**：为 307 行 `editor/rename.ts` 建立 [专项 TTT](./Editor重命名与资源操作职责拆分.ttt.md)。首阶段将资产重命名对话框、请求、Asset 路径同步和 Editor 刷新完整迁入 Asset 子域，四类入口不再加载 Editor rename；共享表单 DOM 收窄保持唯一实现。目标具体值边归零，枚举环/SCC 保持 `719/708`，继续处理剩余菜单回路和 Editor 两类职责。
- **2026-07-26**：Editor 根总网关清除 11 组错层运行时转发，真实消费者直达既有唯一职责所有者；打开编排进入 `editor/open` 子域及其显式网关。`editor/imports.ts` 从唯一 SCC 退出，组件 `708 -> 701`，枚举环因路径重新组合保持 `719`；Node `157/157`，新子域 lint 与目标类型诊断通过。
- **2026-07-26**：Editor 中的系统本地路径打开与资产打开分别迁入真实 Platform/Asset 子域；跨宿主资产导航成为完整 AppFacade 的实例方法，具体实现只在桌面/移动组合根出现。没有以工厂闭包保存 App 或可变状态，跨调用状态继续由统一 Symbol 注册表拥有。唯一 SCC `702 -> 700`，`asset/open`、`platform/localPath` 退出组件，枚举环 `723 -> 718`，Node `157/157` 通过。
- **2026-07-26**：Editor URI 实现进入专属子域，平台块导航上移为完整 App 公共方法；Boot、Agent、菜单和 `openLink` 不再加载 URI 具体实现。移动端动态导入被显式宿主方法替代，两套 URI 的真实语义差异继续分开追踪。唯一 SCC `700 -> 695`，URI 子域、Editor 总网关与 `openLink` 退出，专项 `2/2`、目标诊断 `0`、Node `157/157` 通过。
- **2026-07-26**：为 AI 根目录 22 文件和混合总网关建立 [AI 动作与渲染组合根拆分](./AI动作与渲染组合根拆分.ttt.md)。首批清除 31 个非 AI 错误入口，并将 FileTree Schema、goEnd、Search path handlers、movePath 归入职责子目录；movePath 数据上下文使用完整 `IDialog`，具体 Dialog 只保留在创建边界。唯一 SCC `695 -> 691`，目标类型诊断 `0`、Node `157/157` 通过；AI 内部动作/渲染运行边仍待专项拆分。
- **2026-07-26**：Protyle 菜单聚合根删除，表格菜单进入专项子域，折叠动作归回既有 blockFold 所有者；不保留无消费者 barrel 或为数字拆出的网关。桌面/移动导航历史进一步从两个模块级可变栈收口到统一 Symbol 注册表，完整状态同时包含前进栈与返回方向，不建立按调用点划分的状态接口。移动历史记录/清理由 `MobileBackFoward` 上帝模块迁入独立子域，恢复导航仍保留原实现；最短三节点环消失，唯一 SCC `691 -> 690`，相关导航模块均退出组件。专项 `3/3`、Node `160/160`、目标诊断与新领域 lint 通过。
- **2026-07-26**：确认 Protyle 根 class 为 504 行上帝对象，其中构造器 224 行、消息回调 120 行、事务处理 63 行，创建 [专项 TTT](./Protyle领域根构造与消息生命周期拆分.ttt.md)。首批将退出聚焦纳入经双向校验的完整 `ProtyleDomain`，11 个下层模块不再依赖菜单具体实现；不新增局部 Port。枚举环 `702 -> 700`，SCC 保持 `690`，后续按初始化和消息生命周期继续拆分。
- **2026-07-26**：确认移动键盘工具栏为 334 行上帝模块并创建 [专项 TTT](./移动键盘工具栏状态与生命周期拆分.ttt.md)。跨事件状态从模块变量和初始化闭包迁入统一注册表；关闭、失焦和原生桥接职责进入键盘子域，旧综合工具栏不再作为这些行为的入口。新子域退出 SCC，唯一组件 `690 -> 689`，枚举环 `700 -> 542`；Node `163/163` 和目标类型/lint 通过，剩余工具栏渲染与监听拆分继续滚动。
