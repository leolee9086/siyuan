# 前端上帝对象领域拆分（TikTocTak）

> **最终目标**：将阻碍前端单向依赖的上帝模块按稳定领域职责拆分，使具体 class、全局查询、宿主装配和纯算法不再通过中央聚合对象互相反向依赖，并为“前端循环依赖类型解耦”提供可持续的领域边界。
>
> **当前目标**：将 Editor 应用工厂从具体宿主回边中解耦，并继续拆分 `layout/getAll.ts` 的模型分类和全局环境查询。
>
> **下一步任务**：把 app 级 Editor 运行能力绑定迁到既有模型装配边界，使两个工厂模块退出 SCC；随后提取 `getAll.ts` 的具体模型分类职责。

---

## 关联任务

- 主任务：[前端循环依赖类型解耦](./前端循环依赖类型解耦.ttt.md)
- 本文只追踪必须通过领域拆分才能解除的上帝模块；普通局部反向依赖仍由主任务追踪。
- 每个拆分阶段必须同时更新两份 TTT 的 SCC、简单环、测试和暂存证据。

## 不变量

- 保留并扫描所有 `imports.ts`；通过重新划定领域网关暴露真实依赖，不删除或绕开网关。
- 不以移动文件、动态导入、字符串模块路径、lint 忽略或类型降级掩盖循环。
- 类型可以依赖稳定领域类型；稳定类型不得反向依赖具体实现 class。
- 优先提取领域根与完整能力契约，不按单个调用点堆积细碎接口。
- 不用 `unknown` 代替可参数化的应用、页签、编辑器或模型身份；`unknown` 仅用于真实外部输入校验。
- 相同运行逻辑保持唯一实现；宿主差异确实不相同时允许分开实现。
- 迁移前后保持事件顺序、错误传播、DOM 生命周期、会话状态和公开行为一致。
- 一个拆分阶段只有在相关测试通过且 Tarjan SCC 净下降时才归档；仅改变环路径名称不算完成。

## 识别标准

满足下列多项且在 SCC 中形成大量扇入/扇出的模块，登记为上帝对象或上帝模块：

- 同时承担领域状态、实例创建、全局环境访问、遍历、分类和 UI 副作用。
- 一个 `imports.ts` 聚合多个子域，并被该域的核心 class 反向消费。
- 具体 class 被用于本应由稳定身份、判别字段或参数能力表达的判断。
- 单个模块变化会显著改变 Madge 枚举路径，却不能缩小 Tarjan SCC。
- 调用方为获取一个能力必须依赖完整 `App`、`Protyle`、`Tab`、`Wnd` 或中央注册表。

## 现状基线

- **2026-07-24**：主任务初始 SCC 为 `1020,14,8,4,2,2,2,2`。
- **2026-07-25 当前批次前**：唯一 SCC 为 `922`，Madge 简单环为 `1147`。
- `layout/getAll.ts` 同时包含布局遍历、十余种具体模型 `instanceof` 分类、全局环境读取和面板集合查询。
- `editor/index.ts` 是具体 Editor 模型，却通过 `editor/imports.ts` 依赖全域聚合；该网关反向包含 Layout、Protyle、Dialog、模型查询和 Editor 周边行为。
- `util/file/pathName.ts` 同时承担路径适配、显示名、文件树请求、DOM 动画和笔记本状态；纯消费者会被带入完整 UI 依赖。
- `plugin/index.ts` 的 Plugin class 同时持有插件领域状态和 loader/Dock 装配行为，loader 又必须依赖该 class 的运行时身份。
- 窗口 hash 与 Editor 模型已参数化，`Editor -> setHeader` 直接环归零，公共入口、模型实现和模型网关均已退出 SCC；app 工厂仍在 SCC，当前图为单个 `916`。

## 目标架构

### Editor 模型域

- 具体 Editor class 位于独立模型子域，只依赖 Protyle 创建、配置读取、字数统计、最近文档写入和宿主回调。
- Editor 公共入口只公开模型身份，不聚合 Editor 全域工具。
- Editor 创建工厂绑定宿主能力；窗口 hash 同步作为必需参数，不使用可选 hook 或空实现。
- `instanceof Editor` 仅保留在确实需要具体实现身份的分类边界，普通消费者使用稳定能力契约。

### Layout 查询域

- 纯布局遍历只依赖 `Branch/Window/Tab` 结构泛型，保留真实身份且不导入具体 class。
- 模型分类单独拥有具体模型映射，不与窗口/页签遍历实现混合。
- 全局环境查询负责取得当前布局、移动端和对话框集合，再调用纯遍历或分类函数。
- `getAll.ts` 最终缩减为兼容入口或被明确领域入口替代，不继续拥有全部实现。

## 近期计划

- [ ] **Editor 模型子域**
  - [x] 提取 `IWindowHashModel` 与严格判别身份，Editor/Asset 通过 getter 暴露当前值。
  - [x] 用必需构造能力替代 Editor 对 `setHeader` 的动态导入，直接环数量归零。
  - [x] 将 Editor 具体实现迁出综合网关闭包，建立只覆盖模型构造依赖的子域 `imports.ts`。
  - [x] 更新五个创建入口统一使用参数化工厂，保证所有实例同步语义一致。
  - [x] 参数化应用、页签和编辑引擎身份；Editor 入口、模型实现和模型网关退出应用级 SCC。
  - [ ] 下沉 app 级 Protyle、全屏和窗口 hash 绑定，使两个 Editor 工厂模块退出 SCC。
- [ ] **Layout 查询域**
  - [x] 提取结构化布局遍历唯一实现，并新增嵌套窗口/页签顺序测试。
  - [ ] 将模型分类从 `getAll.ts` 提取为独立具体实现边界。
  - [ ] 将全局环境读取与纯遍历组合迁入查询入口。
  - [ ] 逐步迁移调用方，移除中央模块对无关模型 class 的扇出。
- [ ] **后续候选审计**
  - [ ] `App`：按启动装配、布局宿主、插件宿主和全局事件能力评估。
  - [ ] `Protyle`：按编辑器状态、渲染、选区、工具栏和生命周期评估。
  - [ ] `Menu`/Dock 入口：按状态所有者、条目构建和宿主动作评估。
  - [x] `pathName`：path 与 notebook 两个稳定子域已迁出，文件树请求和 DOM 行为留待后续继续拆分。
  - [x] `Plugin`：loader 生命周期能力已通过必需运行时契约注入，class 不再反向导入 loader。

## 中期计划

- 为每个已确认上帝模块记录入边、出边、状态所有权和可独立测试的领域根。
- 迁移调用方时优先传递完整领域上下文，不创建逐函数参数袋或能力碎片。
- 将具体实现绑定集中在 factory/bootstrap 边界，并验证这些边界自身不进入反向闭环。
- 每完成一个领域，重新读取 `pnpm lint:cycles` 最短环和 Tarjan SCC，决定下一个拆分对象。

## 风险

- **行为漂移**：遍历顺序、未初始化页签、Electron 窗口判断和模型动态值必须保持原语义。
- **形式解耦**：把依赖移动到 factory 或新网关但 SCC 不下降时，不得归档为完成。
- **类型退化**：递归布局和宿主身份若用 `unknown` 或断言拼接，会隐藏真实契约错误。
- **并行修改**：工作树存在大量已暂存变更；只增量暂存本任务文件，不重置其他改动。

## 验收标准

- 对应上帝模块不再同时拥有多个独立领域的运行实现。
- 新领域根拥有直接测试，专项 lint 和 `git diff --check` 通过。
- 每个归档阶段的 Tarjan SCC 相比阶段基线净下降，且没有新增独立 SCC。
- `pnpm lint:cycles` 最终为零；全部 `imports.ts` 保留并参与扫描。
- Node、Vitest、浏览器契约测试及最终 TypeScript 检查全部通过。

## 已完成记录

- **2026-07-25**：建立本 TTT。窗口 hash 身份与纯布局遍历已有唯一实现和契约测试；Editor 模型子域不再依赖具体 App、Tab、Protyle 或全屏实现，公共入口、模型实现和模型网关退出 SCC。Node 测试 `127/127`，新增文件专项 lint 通过；全量类型检查在 8 GB 堆、240 秒内没有诊断但未完成。当前 app 工厂仍在 SCC，唯一组件为 `923`，因此近期阶段继续滚动而不归档。
- **2026-07-25**：`pathName` 首轮拆分完成：路径显示名/POSIX 适配和笔记本状态拥有独立单向子域，旧入口维持导出兼容；3 项路径测试通过。Plugin loader 回边改为 `IPluginRuntime<App, Plugin>` 必需注入，loader 保持唯一重载与 Dock 实现，内部资源插件使用同一运行时工厂。相关纯契约/子域均在 SCC 外；继续按内部边界审计让 `getSavePath`、`fileHtmlGenerator`、`getAll.guard` 和 `ui-utils.environment` 退出组件，应用级 SCC 从 `923` 降至 `919`，简单环为 `1220`。下一步继续拆分 `getAll` 与 app Editor 工厂。
- **2026-07-25**：边界叶节点继续通过稳定领域依赖退出组件：拼音转换由专用网关持有；更新日志由宿主注入完整 Dialog Port，不再让 boot 网关依赖具体 Dialog class。唯一 SCC 从 `919` 降至 `916`，简单环仍为 `1220`，Node `130/130`。这两项不替代 Editor/Layout 上帝模块拆分，近期任务保持未完成。
