# S-Forge 素材管理系统执行跟踪 (TikTocTak)

> **目标**: 实现增强的素材管理系统,支持从Eagle等外部素材库导入、标签管理和筛选、百万级素材流畅浏览,与思源笔记无缝集成。量化指标:支持百万级素材流畅浏览(60fps),Eagle库导入成功率>95%。
>
> **流程**: 这是一个滚动更新的执行路线图。
> 1. 从"近期计划"中认领一个任务。
> 2. 完成开发和测试。
> 3. 将其移动到"已归档/已完成"区域。
> 4. 将"中期计划"中的条目提升到"近期计划"。

---

## 🎯 核心原则

### 架构设计原则

1. **数据存储策略(与思源一致)**
   - `meta.json` 是唯一数据源,可同步、可版本控制
   - SQLite 索引仅用于加速查询,可随时从 JSON 重建
   - 数据存储路径: `workspace/data/storage/s-forge-asset-meta/meta.json`
   - 索引存储路径: `workspace/temp/s-forge-asset-meta.db` (不同步)

2. **性能要求**
   - 百万级素材流畅浏览(60fps)
   - 查询响应时间 < 100ms
   - 导入速度 > 1000个素材/分钟

3. **数据安全**
   - 路径安全验证(防止路径遍历攻击)
   - 元数据完整性自动修复
   - 支持索引重建

### 验证检查清单

每个Phase完成后必须验证:
- [ ] 功能正确性:单元测试覆盖率 > 80%
- [ ] 性能达标:符合上述性能要求
- [ ] 数据安全:通过路径安全验证
- [ ] 文档完整:API文档和使用说明齐全
- [ ] 代码质量:通过lint检查

---

## ℹ️ 如何维护此文档

1. **完成归档**:任务完成后,**必须**剪切粘贴到【已归档】列表,并打上 `[x]` 和日期。
2. **补充弹药**:当【近期计划】空了,从【中期计划】里挑选任务挪上去。
3. **因地制宜**:如果发现计划不合理,随时修改或删除。
4. **数据驱动**:用数据说话,不凭感觉。

---

## 🟢 近期计划 (立即聚焦,撸起袖子干)

- [ ] **Phase 2.1: 查询API实现 (P0)**
  - **背景**: 前端需要查询素材列表、单个素材详情和高级搜索功能
  - **行动**:
    1. 实现 `POST /api/s-forge/asset-meta/list` - 分页列表查询
       - 支持参数: limit(默认200), offset, orderBy(CREATEDATE/FILESIZE/NAME/RESOLUTION)
       - 支持过滤: keyword, ext, tags, star
    2. 实现 `POST /api/s-forge/asset-meta/get` - 获取单个素材
    3. 实现 `POST /api/s-forge/asset-meta/search` - 高级搜索(支持颜色)
  - **验收标准**:
    - [ ] 所有API端点正常响应
    - [ ] 查询响应时间 < 100ms (10万素材测试)
    - [ ] 支持所有过滤参数
    - [ ] 单元测试覆盖率 > 80%
  - **参考文档**: [`toread/forEagle/useEagleItem.js`](../../toread/forEagle/useEagleItem.js)

- [ ] **Phase 2.2: 修改API实现 (P0)**
  - **背景**: 需要支持素材元数据的创建、更新和批量操作
  - **行动**:
    1. 实现 `POST /api/s-forge/asset-meta/set` - 设置/更新单个素材
       - 支持字段: path(必需), name, tags, star(0-5), annotation
    2. 实现 `POST /api/s-forge/asset-meta/batch-set` - 批量设置
  - **验收标准**:
    - [ ] 支持单个和批量更新
    - [ ] 数据验证正确(star范围、路径安全等)
    - [ ] 自动更新SQLite索引
    - [ ] 单元测试覆盖率 > 80%
  - **参考文档**: [`kernel/assetmeta/model.go`](../../kernel/assetmeta/model.go)

- [ ] **Phase 2.3: 标签API实现 (P1)**
  - **背景**: 需要统一管理素材标签和思源标签
  - **行动**:
    1. 实现 `GET /api/s-forge/asset-meta/tags` - 获取所有标签(合并思源标签)
    2. 实现 `POST /api/s-forge/asset-meta/tags/set` - 设置标签颜色
  - **验收标准**:
    - [ ] 正确合并素材标签和思源标签
    - [ ] 支持标签颜色设置
    - [ ] 标签查询性能 < 50ms
  - **参考文档**: 文档第9节"标签系统协调"

---

## 🟡 中期计划 (架构演进,步步为营)

- [ ] **Phase 2.4: 索引管理API (P1)**
  - **背景**: 需要提供索引重建和状态查询功能
  - **行动**:
    1. 实现 `POST /api/s-forge/asset-meta/rebuild-index` - 强制重建索引
    2. 实现 `GET /api/s-forge/asset-meta/status` - 获取索引状态
  - **参考文档**: [`kernel/assetmeta/index.go`](../../kernel/assetmeta/index.go)

- [ ] **Phase 3.1: Eagle库检测 (P1)**
  - **背景**: 需要验证Eagle库的有效性并提供库信息
  - **行动**:
    1. 实现 `DetectLibrary` 函数检测是否为有效Eagle库
    2. 实现 `POST /api/s-forge/asset-meta/import/eagle/detect` API
    3. 解析库元数据(名称、素材数量、文件夹数量、标签数量)
  - **验收标准**:
    - [ ] 正确识别.library目录
    - [ ] 准确统计库信息
    - [ ] 错误处理完善
  - **参考文档**: [`toread/forEagle/fromEagleFs.js`](../../toread/forEagle/fromEagleFs.js)

- [ ] **Phase 3.2: Eagle元数据映射 (P1)**
  - **背景**: 需要将Eagle的元数据结构映射到我们的数据模型
  - **行动**:
    1. 定义Go结构体: `EagleItemMeta`, `EagleLibraryMeta`, `EagleFolder`, `TagGroup`
    2. 实现元数据读取函数: `readLibraryMeta`, `readItemMeta`
    3. 实现字段映射逻辑(id→sourceId, folders→tags等)
  - **验收标准**:
    - [ ] 所有关键字段正确映射
    - [ ] 支持调色板(palettes)数据
    - [ ] 处理可选字段(url, annotation等)
  - **参考文档**: 文档第3.3节"素材元数据映射"

- [ ] **Phase 3.3: Eagle标签和文件夹导入 (P1)**
  - **背景**: Eagle的文件夹结构需要转换为标签系统
  - **行动**:
    1. 实现 `importTags` - 读取tags.json和metadata.json中的tagsGroups
    2. 实现 `mapFolders` - 解析folders树结构,转换为标签前缀
    3. 合并到我们的tags.json
  - **验收标准**:
    - [ ] 文件夹层级正确转换为标签前缀(如"eagle/设计/图标")
    - [ ] 标签颜色正确保留
    - [ ] 避免标签重复

- [ ] **Phase 3.4: Eagle素材批量导入 (P0)**
  - **背景**: 核心功能,需要高效导入大量素材
  - **行动**:
    1. 实现 `importItems` - 遍历images/*.info/目录
    2. 支持两种模式: 复制文件模式和外部库模式
    3. 实现进度跟踪和错误处理
    4. 批量更新SQLite索引
  - **验收标准**:
    - [ ] 导入速度 > 1000个素材/分钟
    - [ ] 导入成功率 > 95%
    - [ ] 支持断点续传
    - [ ] 详细的错误报告
  - **参考文档**: 文档第3.5节"实现步骤"

- [ ] **Phase 3.5: Eagle导入API和进度查询 (P1)**
  - **背景**: 前端需要启动导入和查询进度
  - **行动**:
    1. 实现 `POST /api/s-forge/asset-meta/import/eagle/start` - 开始导入
    2. 实现 `GET /api/s-forge/asset-meta/import/status` - 导入进度查询
    3. 支持导入选项: targetDir, copyFiles, includeTags, excludeTags, syncMode
  - **验收标准**:
    - [ ] 支持异步导入
    - [ ] 实时进度更新
    - [ ] 支持取消导入

---

## 🔴 远期计划 (北极星目标,星辰大海)

- [ ] **Phase 4.1: 素材选择面板增强 (P2)**
  - **愿景**: 提供强大的素材筛选和管理界面
  - **功能**: 属性过滤(格式、分辨率、文件大小、星级)、标签筛选UI、元数据编辑面板

- [ ] **Phase 4.2: Eagle导入向导UI (P2)**
  - **愿景**: 提供友好的Eagle库导入向导
  - **功能**: 库选择、预览、导入选项配置、进度显示

- [ ] **Phase 5: 颜色相似查询优化 (P2)**
  - **愿景**: 实现高效且符合视觉感知的颜色查找
  - **技术**: HSV空间粗筛 + CIEDE2000精排
  - **参考文档**: 文档第8节"颜色相似查询实现"

- [ ] **Phase 6: 元数据绑定功能 (P2)**
  - **愿景**: 将素材描述绑定到思源区块
  - **功能**: BoundBlockID字段、双向同步、复用文档块
  - **参考文档**: 文档第10节"元数据绑定"

- [ ] **Phase 7: 更多导入适配器 (P2)**
  - **愿景**: 支持更多素材库格式
  - **格式**: 本地文件夹扫描、Pinterest导出、Figma导出

---

## 🏁 已归档/已完成

- [x] **Phase 1: 基础设施** [已完成 2026-01-XX]
  - **背景**: 建立素材管理系统的核心基础设施
  - **完成情况**:
    - 创建 `assetmeta` 包 (`kernel/assetmeta/`)
    - 实现 JSON 数据源读写 (`model.go`: `LoadAsset`, `SaveAsset`, `RemoveAsset`)
    - 实现 SQLite 索引管理 (`index.go`: 完整的表结构和 CRUD)
    - 索引重建逻辑 (`RebuildIndex`, `BatchUpdateIndexAssets`)
    - 路径安全验证 (`isPathInsideDir`, `ErrPathTraversal`)
    - 元数据完整性修复 (自动补全 0 值属性)
    - 色彩提取性能优化 (避免重复计算 MMCQ)
  - **成果文件**:
    - [`kernel/assetmeta/model.go`](../../kernel/assetmeta/model.go) (266行)
    - [`kernel/assetmeta/service.go`](../../kernel/assetmeta/service.go) (437行)
    - [`kernel/assetmeta/index.go`](../../kernel/assetmeta/index.go) (392行)
    - [`kernel/assetmeta/mmcq.go`](../../kernel/assetmeta/mmcq.go) (482行)
  - **参考文档**: 原plan.md Phase 1章节

---

## 📊 进度跟踪

### 整体进度
- Phase 1 (基础设施): ✅ 100% 已完成
- Phase 2 (API层): 🔄 0% 进行中
- Phase 3 (Eagle导入): 📋 0% 待开始
- Phase 4 (前端集成): 📋 0% 待开始

### 关键里程碑
- [x] 2026-01-XX: Phase 1 基础设施完成
- [ ] 预计 2026-02-XX: Phase 2 API层完成
- [ ] 预计 2026-03-XX: Phase 3 Eagle导入完成
- [ ] 预计 2026-04-XX: Phase 4 前端集成完成

---

## 📚 技术参考

### 数据结构

#### meta.json 结构
```json
{
  "version": 1,
  "assets": {
    "assets/xxx.png": {
      "name": "图标设计",
      "tags": ["设计", "图标", "UI"],
      "star": 3,
      "annotation": "从 Eagle 导入",
      "source": "eagle",
      "sourceId": "ABCD1234",
      "importTime": 1234567890,
      "width": 1920,
      "height": 1080
    }
  },
  "tags": {
    "设计": { "color": "#FF6B6B" },
    "图标": { "color": "#4ECDC4" }
  }
}
```

#### SQLite 索引表结构
```sql
-- 素材元数据索引
CREATE TABLE asset_meta (
    path TEXT PRIMARY KEY,
    name TEXT,
    source TEXT,
    star INTEGER DEFAULT 0,
    import_time INTEGER,
    width INTEGER,
    height INTEGER,
    file_size INTEGER
);

-- 标签关联(多对多)
CREATE TABLE asset_tags (
    path TEXT,
    tag TEXT,
    PRIMARY KEY(path, tag)
);
CREATE INDEX idx_asset_tags_tag ON asset_tags(tag);
CREATE INDEX idx_asset_tags_path ON asset_tags(path);

-- 标签定义
CREATE TABLE tags (
    name TEXT PRIMARY KEY,
    color TEXT
);

-- 调色板存储
CREATE TABLE asset_palettes (
    path TEXT,
    r INTEGER, g INTEGER, b INTEGER,
    h INTEGER, -- Hue: 0-360
    s INTEGER, -- Saturation: 0-100
    l INTEGER, -- Lightness: 0-100
    ratio REAL,
    FOREIGN KEY(path) REFERENCES asset_meta(path)
);
CREATE INDEX idx_palette_h ON asset_palettes(h);
CREATE INDEX idx_palette_sl ON asset_palettes(s, l);
```

### Eagle 文件系统结构
```
MyLibrary.library/              # 库根目录(.library结尾)
├── metadata.json               # 库元数据(folders, smartFolders, tagsGroups)
├── tags.json                   # 标签定义(可选)
└── images/                     # 素材存储目录
    └── <ID>.info/              # 单个素材目录
        ├── metadata.json       # 素材元数据
        └── <ID>.<ext>          # 原始文件
```

### 依赖项
- 缩略图服务 ✅ 已完成
- 现有 assets 目录结构
- 百万级文件视图组件

---

**文档创建**: Roo (Code Mode)  
**最后更新**: 2026-01-26  
**原始文档**: [`docs/S-FORGE-ASSET-MANAGER.plan.md`](../S-FORGE-ASSET-MANAGER.plan.md)  
**版本**: 1.0.0
