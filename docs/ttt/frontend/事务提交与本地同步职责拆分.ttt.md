# 事务提交与本地同步职责拆分（TikTocTak）

## 最终目标

将 Protyle 事务的命令提交、撤销登记、本地 DOM 同步、内核请求和 transaction 辅助操作拆为单向职责；保留全部公开函数、事务顺序、undo 数据、编辑器局部同步和回调时序，使只需提交事务的交互模块不加载 turns、fold、onTransaction 或完整 Protyle 渲染运行时。

## 当前目标

- [x] 登记 `transaction.ts` 综合出口和 `transaction.promise.ts` 双向依赖基线。
- [x] 提取 `transaction/removeTopElement` 唯一命令实现，删除无消费者的综合根入口。
- [x] 将所有生产调用点直达真实事务职责所有者。
- [x] 消除 submit/promise 与 promise/removeTopElement 的直接模块互递。

## 下一步任务

1. 拆分 `transaction.promise.ts` 的本地 DOM 同步阶段与内核请求阶段。
2. 消除 `submit -> imports -> promise -> submit` 真实互递，使提交入口退出 SCC。
3. 扩展请求队列、回调与嵌入块延迟刷新行为测试。

## 不变量

- `transaction` 的同步返回、空操作短路、无 Protyle 请求、undo 登记、lite 分支和 observer 断开顺序保持不变。
- 不以工厂闭包、动态导入、事件总线或调用点回调隐藏递归依赖。
- 模块级事务队列和跨调用状态继续由现有统一注册表/队列所有者管理。
- 不创建按调用点裁剪的事务 Port；函数职责使用完整请求与结果类型。
- `imports.ts` 继续存在并直接指向事务真实所有者，不经过其它网关。

## 现状基线

- `transaction.ts` 公开 `transaction/removeTopElement/updateTransaction/updateBatchTransaction`，并聚合 turns、fold、onTransaction 出口。
- 全仓约 125 个模块从综合 transaction 路径导入；只需 `transaction` 的调用也会静态加载所有 re-export。
- `transaction.promise.ts` 负责大规模本地 DOM 同步和内核提交，并反向导入根模块的 `transaction/removeTopElement`，形成直接模块循环。
- 当前唯一 SCC `679`，枚举环 `539`；dragFill 最短路径经 `transaction.turns -> block/imports -> Protyle -> mousedown` 返回。

## 近期计划

- [x] 综合 transaction 入口不再是提交调用方的运行时依赖。
- [x] transaction 各符号的生产导入均直达唯一所有者。

## 中期计划

- [ ] 将 promise 模块的 update/delete/move/insert 与网络提交职责拆成单向阶段。
- [ ] `transaction.submit` 与本地同步实现之间不存在模块互递。

## 远期计划

- [ ] 与 [前端循环依赖类型解耦](./前端循环依赖类型解耦.ttt.md) 一并完成 Protyle 事务相关 SCC 归零验收。

## 风险与验收标准

- 测试覆盖同编辑器顺序、不同编辑器隔离、失败可观察、undo 登记、空操作、lite 与回调。
- 所有迁移导入解析到相同函数对象，不复制实现或建立兼容分叉。
- 新模块 lint、目标类型诊断、Node/Vitest、imports 多跳门禁、循环图和 diff 校验通过。
- 全量类型检查中的既存诊断单独记录，不用宽泛类型或断言降低类型能力。

## 已完成记录

- **2026-07-26**：AV `cell.ts` 聚合入口退出 SCC 后，最短路径转为 `dragFill -> transaction -> transaction.turns -> block/imports -> Protyle -> mousedown -> dragFill`。审计确认根模块聚合提交与三组辅助出口，promise 模块反向导入根提交函数；创建本专项 TTT，按真实职责分阶段处理，不对 dragFill 注入事务回调。
- **2026-07-26**：建立 `transaction/submit.ts` 唯一提交与顶层删除实现、`transaction/update.ts` 更新实现、子域直达网关和共享身份验证器。使用 TypeScript AST 按导入符号将 124 个文件、130 条旧根导入拆到 submit/update/turns/fold/onTransaction 真实所有者；未知符号、默认导入和命名空间导入会使迁移中止。旧 `wysiwyg/transaction.ts` 生产引用清零后删除，没有保留 lint 禁止的 barrel。新增边界对 config、undo、wysiwyg 和事务 ID 显式验证，不使用断言或宽泛类型；新子域 lint、模块解析与严格类型诊断为 `0`。事务专项 `3/3` 覆盖空操作、undo/lite 与非 lite/observer，Node `175/175`、`typecheck:protyle-contract`、imports 多跳和 diff 校验通过。完整 Vitest 首批含专项 `44/44`，第二批被未修改的 `packages/caliburRouter` `有交集().filter` 回归阻断。源码图 `2181` 节点、枚举环 `539 -> 531`；submit/imports/update 仍因 promise 互递进入唯一 SCC，规模阶段性 `679 -> 681`，下一阶段继续拆本地同步实现。
- **2026-07-26**：顶层 DOM 删除拆为 `removeTopElementAndCollectOperations`，submit 保留原提交包装，promise 对无 undo 后续操作直接复用自身执行语义并共享唯一插入观察器后置步骤，不再反向导入 submit。首次实现复用了 transaction 根网关，依赖图立即暴露 `promise -> removeTopElement -> imports -> promise` 假回路；复核后将 removeTopElement 与 update 各自归入独立子域和直达网关，根网关只保留 submit 确实需要的 Constants/fetch/promise。submit/promise 与 promise/removeTopElement 直接环归零，removeTopElement 子域退出 SCC；枚举环 `531 -> 529`，新增明确职责节点使唯一 SCC 阶段性 `681 -> 682`。事务专项扩展为 `4/4`，覆盖嵌套顶层删除、操作收集与非根 zoomOut；Node `175/175`、Protyle 契约、事务子域 lint/严格类型、imports 多跳和 diff 校验通过。当前最短返回路径为 `promise -> av/render -> locate -> submit -> imports -> promise`，下一步拆 promise 的高层渲染依赖。
