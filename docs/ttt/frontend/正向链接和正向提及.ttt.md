# 正向链接和正向提及执行跟踪 (TikTocTak)

> **目标**: 实现正向链接(Forwardlinks)和正向提及(Forward Mentions) Dock功能，显示当前笔记引用的块列表，与反向链接形成完整的引用关系视图。量化指标：完成核心组件开发、界面集成、国际化支持，通过功能测试。
>
> **流程**: 这是一个滚动更新的执行路线图。
> 1. 从"近期计划"中认领一个任务。
> 2. 完成开发和测试。
> 3. 将其移动到"已归档/已完成"区域。
> 4. 将"中期计划"中的条目提升到"近期计划"。

---

## 📋 核心原则

### 功能定义
1. **正向链接 (Forwardlinks)**: 显示当前笔记中存在块引用的块列表，即"我引用了谁"
2. **正向提及 (Forward Mentions)**: 显示当前笔记中存在文本提及的块列表（与锚文本匹配的文档/块）
3. 外观与反向链接 Dock 保持一致

### 概念对比

| 概念 | 反向链接 (Backlink) | 正向链接 (Forwardlink) |
|------|---------------------|------------------------|
| 问题 | "谁引用了我？" | "我引用了谁？" |
| refs表查询条件 | `def_block_id = 当前块ID` | `root_id = 当前文档RootID` |
| 返回内容 | 引用来源块列表 | 引用目标块列表 |

### 技术架构

#### 核心参考文件

| 文件 | 说明 |
|------|------|
| [`app/src/layout/dock/Backlink.ts`](../../app/src/layout/dock/Backlink.ts) | 反向链接实现，主要参考模板 |
| [`app/src/layout/dock/dock.factory.ts`](../../app/src/layout/dock/dock.factory.ts) | Model 工厂，需添加 `forwardlink` 类型 |
| [`app/src/util/Tree.ts`](../../app/src/util/Tree.ts) | 树形列表组件 |
| `kernel/sql/block_ref_query.go` | refs表查询逻辑参考 |

#### refs 表结构

```go
// scanRefRows 中的字段顺序
ID              // 引用记录ID
DefBlockID      // 被引用块ID (定义块)
DefBlockParentID // 被引用块父ID
DefBlockRootID  // 被引用块所属文档ID
DefBlockPath    // 被引用块路径
BlockID         // 引用块ID (包含引用的块)
RootID          // 引用块所属文档ID
Box             // 笔记本ID
Path            // 路径
Content         // 锚文本内容
Markdown        // Markdown 内容
Type            // 引用类型
```

#### 数据获取策略

**方案选择：使用 SQL API 前端实现**

1. **短期方案**：在前端使用 `/api/query/sql` 直接查询 refs 表
2. **长期方案**：后续可添加后端 API `/api/ref/getForwardlink` 以提升性能

**正向链接 SQL 查询**：
```sql
-- 获取当前文档引用的所有目标块（按文档分组）
SELECT DISTINCT 
    r.def_block_root_id,
    b.content,
    b.hPath,
    b.box
FROM refs AS r
INNER JOIN blocks AS b ON b.id = r.def_block_root_id
WHERE r.root_id = ?  -- 当前文档 rootID
ORDER BY b.hPath
```

**正向提及策略**：
- 获取当前文档的标题、命名、别名
- 搜索其他文档中包含这些关键词的块

### 验收检查清单

- [ ] 正向链接列表正确显示当前文档引用的其他文档/块
- [ ] 点击列表项能正确跳转到目标块
- [ ] Ctrl+Click / Alt+Click / Shift+Click 行为与反向链接一致
- [ ] 刷新、展开、折叠等操作正常
- [ ] 切换编辑器时正确更新显示内容
- [ ] 状态能正确保存和恢复

---

## ℹ️ 如何维护此文档

1. **完成归档**：任务完成后，**必须**剪切粘贴到【已归档】列表，并打上 `[x]` 和日期。
2. **补充弹药**：当【近期计划】空了，从【中期计划】里挑选任务挪上去。
3. **因地制宜**：如果发现计划不合理，随时修改或删除。
4. **数据驱动**：用数据说话，不凭感觉。

---

## 🟢 近期计划 

- [ ] **Phase 1.1: 创建 Forwardlink.ts 核心组件 (P0)**
  - **背景**: 正向链接功能的核心实现，需要复制 [`Backlink.ts`](../../app/src/layout/dock/Backlink.ts) 的结构并修改关键逻辑
  - **行动**:
    1. 创建 [`app/src/layout/dock/Forwardlink.ts`](../../app/src/layout/dock/Forwardlink.ts) 文件
    2. 复制 [`Backlink.ts`](../../app/src/layout/dock/Backlink.ts) 的基础结构
    3. 修改类名为 `Forwardlink`
    4. 修改 CSS class: `sy__forwardlink`
    5. 修改列表容器 class: `forwardlinkList`, `forwardlinkMList`
    6. 实现 `searchForwardlinks()` 方法，使用 SQL 查询 `WHERE root_id = this.rootId`
    7. 调整 `render()` 方法以渲染正向链接数据
  - **验收标准**:
    - [`Forwardlink.ts`](../../app/src/layout/dock/Forwardlink.ts) 文件创建成功
    - 类结构完整，包含所有必需属性和方法
    - SQL 查询逻辑正确实现
    - 代码通过 TypeScript 编译
  - **参考文档**: [`app/src/layout/dock/Backlink.ts`](../../app/src/layout/dock/Backlink.ts)

- [ ] **Phase 1.2: 修改 dock.factory.ts 注册工厂 (P0)**
  - **背景**: 需要在 Dock 工厂中注册 `forwardlink` 类型，使其能被正确实例化
  - **行动**:
    1. 打开 [`app/src/layout/dock/dock.factory.ts`](../../app/src/layout/dock/dock.factory.ts)
    2. 导入 `Forwardlink` 类
    3. 创建 `initForwardlink` 工厂函数
    4. 将 `forwardlink` 添加到 `MODEL_FACTORIES` 对象
  - **验收标准**:
    - 工厂函数正确实现
    - `forwardlink` 类型能被正确识别和实例化
    - 代码通过 TypeScript 编译
  - **参考文档**: [`app/src/layout/dock/dock.factory.ts`](../../app/src/layout/dock/dock.factory.ts)

- [ ] **Phase 1.3: 实现 SQL 查询逻辑 (P0)**
  - **背景**: 需要通过 SQL API 查询 refs 表获取正向链接数据
  - **行动**:
    1. 在 [`Forwardlink.ts`](../../app/src/layout/dock/Forwardlink.ts) 中实现 SQL 查询
    2. 使用 `/api/query/sql` 接口
    3. 实现数据转换逻辑，将查询结果转换为树形结构
    4. 处理错误情况
  - **验收标准**:
    - SQL 查询能正确返回当前文档引用的目标块
    - 数据转换逻辑正确
    - 错误处理完善
  - **参考文档**: `kernel/sql/block_ref_query.go`

---

## 🟡 中期计划 

- [ ] **Phase 2.1: 添加国际化支持 (P1)**
  - **背景**: 需要添加多语言支持，包括 `forwardlinks` 和 `forwardMentions` 键
  - **行动**:
    1. 检查 [`app/src/types/i18n.types.ts`](../../app/src/types/i18n.types.ts)
    2. 添加 `forwardlinks: "正向链接"` 和 `forwardMentions: "正向提及"` 键
    3. 或使用 forgeI18n 扩展机制
    4. 更新所有语言文件

- [ ] **Phase 2.2: 添加 Dock 按钮配置 (P1)**
  - **背景**: 需要在 Dock 中添加正向链接按钮，使用户能够打开正向链接面板
  - **行动**:
    1. 在 Dock 初始化逻辑中添加 `forwardlink` 按钮配置
    2. 设置图标（可考虑使用 `#iconLink` 的变体）
    3. 设置 hotkeyLangId 和 title
    4. 测试按钮点击行为

- [ ] **Phase 2.3: 样式调整 (P1)**
  - **背景**: 确保正向链接面板的样式与反向链接保持一致
  - **行动**:
    1. 复制反向链接的 CSS 样式
    2. 修改 class 名称为 `sy__forwardlink`
    3. 调整必要的样式差异
    4. 测试不同主题下的显示效果

---

## 🔴 远期计划 

- [ ] **Phase 3.1: 正向提及实现 (P2)**
  - **愿景**: 实现正向提及功能，显示当前文档标题/别名在其他文档中的提及

- [ ] **Phase 3.2: 排序/过滤功能 (P2)**
  - **愿景**: 添加排序和过滤选项，提升用户体验

- [ ] **Phase 3.3: 状态保存/恢复 (P2)**
  - **愿景**: 保存用户的展开/折叠状态，下次打开时恢复

- [ ] **Phase 4.1: 后端 API 支持 (P3)**
  - **愿景**: 添加后端 API `/api/ref/getForwardlink` 以提升性能

- [ ] **Phase 4.2: 缓存优化 (P3)**
  - **愿景**: 实现查询结果缓存，减少重复查询

---

## 🏁 已归档/已完成

- [x] **Phase 0: 分析 Backlink.ts 结构** [已完成 2026-01-26]
  - **背景**: 需要理解反向链接的实现方式，作为正向链接开发的参考
  - **完成情况**: 已完成 [`Backlink.ts`](../../app/src/layout/dock/Backlink.ts) 结构分析，明确了核心属性、方法和数据流
  - **成果文件**: 本文档的技术架构章节
  - **参考文档**: [`app/src/layout/dock/Backlink.ts`](../../app/src/layout/dock/Backlink.ts)

---

## 📊 进度跟踪

- **总体进度**: 5% (1/20 任务完成)
- **当前阶段**: Phase 1 - 核心组件开发
- **下一里程碑**: 完成 Phase 1.1-1.3，实现基础正向链接功能

### 文件变更清单

| 操作 | 文件路径 | 状态 | 说明 |
|------|----------|------|------|
| 新增 | [`app/src/layout/dock/Forwardlink.ts`](../../app/src/layout/dock/Forwardlink.ts) | 待开发 | 正向链接主组件 |
| 新增 | [`app/src/layout/dock/forwardlink.api.ts`](../../app/src/layout/dock/forwardlink.api.ts) | 可选 | API 封装 |
| 修改 | [`app/src/layout/dock/dock.factory.ts`](../../app/src/layout/dock/dock.factory.ts) | 待修改 | 添加工厂函数 |
| 修改 | [`app/src/layout/dock/dock.init.ts`](../../app/src/layout/dock/dock.init.ts) | 待修改 | 添加按钮配置 |
| 修改 | [`app/src/types/i18n.types.ts`](../../app/src/types/i18n.types.ts) | 待修改 | 添加国际化键 |

---

**文档创建**: 2026-01-26  
**最后更新**: 2026-01-26  
**原始文档**: [`docs/正向链接和正向提及实现计划.plan.md`](../正向链接和正向提及实现计划.plan.md)
