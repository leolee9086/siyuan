# 上游逐提交语义合并执行跟踪（滚动 U1，当前 0fd89fde3c9c）

> **最终目标**：按照 `siyuan://blocks/20260729083130-vaxfqpr`，将官方 SiYuan `dev` 从 `86b4734f0bd2c9046de97fd29cae47c8fb308fdb` 到每个 series 边界获取的最新完整 tip 语义迁入 S-Forge；当前 `U1=0fd89fde3c9c0240172eee0a81352759c391d292`。不得丢失任一上游或本地功能，以本地领域结构为实现骨架，把每个已验证 series 持续交付到主线，最终完成真实双亲拓扑 merge `M`。
>
> **当前目标**：`D001` 已完成 Git、Kernel 热切换、前端当前性和六入口健康闭合；滚动刷新工具已进入主线。官方 `dev` 已连续快进到 `0fd89fde...`，Issue `8554` / `13396` 引用块删除与剪切 series 已在候选分支完成语义迁移、审计决策和定向回归，等待主线的受保护热替换重新获批并完成运行时新鲜度验收后，以短时 `--no-ff` 集成。
>
> **下一步任务**：提交增量审计工具、刷新后的清单与证据；在最新主线将已验证的 Issue `8554` / `13396` series 与主线合并，重跑受影响测试，并在受控热切换和六入口验证后形成 `D002`。该 delivery 闭合后，为 Codex 配置 S-Forge MCP 并执行一次只读健康调用，再继续最早未闭合的 Issue `8554` 键盘、粘贴、格式化和列表转换行为。

---

## 不变量

1. 上游读取、意图审计、迁移实现和交付前验证只在 `D:/dev/s-forge-upstream-sync-20260729-7845c47e3` 独立完整克隆中执行；主仓库 `D:/dev/s-forge` 只在短时集成窗口接纳已验证 series，不再固定于 `L0` 等待整轮完成。
2. `L0`、`U0`、`U1` 使用完整 SHA；`U0` 是 `U1` 的祖先，范围必须包含 merge、测试、生成物、依赖和配置提交。
3. `commits.jsonl` 当前 544 个唯一 SHA 是本次 series 的闭合范围。每个新 series 建立前及最终闭合前必须刷新官方 `dev`；快进则扩展同一清单，非快进则停止并审计历史改写。执行分组可省略或按领域动态建立，不存在固定十提交门槛，分组完成不代表整体完成。
4. 每项必须从 `pending` 进入规程允许的明确终态；补丁相同、文本搜索、无冲突、类型通过或构建通过均不单独证明语义等价。
5. 本地结构是实现骨架。上游单体文件已在本地拆分时，应把行为迁入真实领域所有者，不复活旧入口；功能差异不适合共享上下文时不强行合并实现。
6. 一个上游提交可映射多个本地原子提交，不可分割 series 可多对一；每条本地语义提交必须有连续、可解析的来源 trailers。
7. 任一失败都写入本文和 `verification.json`，不得静默回退、跳过依赖项或通过调整分组隐藏失败。
8. 每个可独立验证的 series 必须及时以 `D_i` 接入主线并记录父关系、验证和回滚边界；不得囤积到完整清单全部完成后一次性交付。
9. 只有最终刷新后的完整清单全部闭合且完整门禁通过后才创建 `M`；`tree(M) == tree(P)`、`M^1 == P`、`M^2 == U1`。`M` 只记录上游祖先边界，不承载延迟交付的业务代码。
10. 本地常驻 S-Forge 必须在整个同步周期保持健康并使用最新主线运行时语义。Git 合入后的 delivery 先标记 `git-integrated`；前端当前性、Kernel Supervisor 热替换、版本新鲜度和关键页面探针全部通过后才标记 `integrated`，否则停止下一 series。
11. 版本化 `pre-commit/post-commit` hooks 自动执行分层验证：前者只验证暂存差异的格式与固定测试，后者请求运行时热切换。每个后端 delivery 必须在实际热切换后才可闭合；crash、候选失败和回退必须形成不含凭据的结构化 incident。未闭合 deployment 阻断 delivery `integrated`，后置 hook 记录摘要后以成功状态返回，不阻断下一次独立提交。
12. `pnpm forge` 只允许从工作树、索引和未跟踪文件集合均为空的主线启动；清洁检查必须先于 hooks 安装、端口选择、Supervisor 复用或 Kernel 启动。

## 冻结基线

| 项目 | 值 |
|---|---|
| 周期 ID | `siyuan-dev-28e38647fb02` |
| 隔离候选分支 | `sync/upstream-dev-72987919-issue-8554` |
| 主线分支 | `multipleAI`，允许在审计期间继续接纳普通功能提交 |
| 交付方式 | `D1...Dn` 滚动 `--no-ff` 集成；最终仅创建一次拓扑 `M` |
| 运行时门禁 | 每个 `D_i` 后更新本地前端与 Kernel；旧核心在构建/测试期间继续服务，失败时保留或恢复上一健康版本 |
| 提交自动化 | `pre-commit` 仅验证暂存差异的格式与固定测试，`post-commit` 自动触发后端热切换并持久化 deployment/incident；部署状态不作为下一次提交条件，后置失败不改变 Git 提交结果 |
| `L0` | `7845c47e3620a7cd411648e36fd508c43ea757f7` |
| `U0` | `86b4734f0bd2c9046de97fd29cae47c8fb308fdb` |
| `U1` | `0fd89fde3c9c0240172eee0a81352759c391d292` |
| 上游获取时间 | `2026-07-30T21:34:00+08:00`；每个 series 边界重新获取 |
| 候选分支创建时间 | `2026-07-29T14:15:25+08:00` |
| 完整 DAG | 546 个提交，含 41 个 merge commit |
| Stable patch ID | 505 个普通提交可计算 |
| 当前终态 | 8 `verified`，538 `pending` |
| 发布规程块 | `20260729083130-vaxfqpr`，更新于 `20260730090212` |
| 发布规范化 Kramdown SHA-256 | `25c5255f6578c935452baedfbfdb03904843b2fb2421656f7a3e5f5193e67f62`；LF 换行、IAL 属性字典序 |
| 仓库镜像 SHA-256 | `2cbe92e9d456950111b341cbebf1658c6c121ab76493b14bcf3ef5ab03a43573` |

周期 ID 和现有审计路径保持稳定，以保证既有 commit trailers、D001 和审计证据可持续引用；`cycle.json.upstreamTip` 才是当前权威 `U1`，文件名中的旧短 SHA 不再解释为当前 tip。

`U0` 来自已完成回归的上一次真实 integration merge `24cb30973477...` 的第二父；上一轮验证证据见 `docs/ttt/版本管理/远程分支合并_multipleAI_86b4734f0.ttt.md`。

## 近期计划

- [x] **Phase 0：主线原子收口与隔离环境**
  - 主线历史已消除 `fixup!` / `squash!`，Node 228/228、Vitest 842/842 通过；主仓库保持干净。
  - 从 `L0` 建立独立完整克隆和候选分支，安装隔离前端依赖，添加官方 `upstream` 远端。
- [x] **Phase 1：规程修订与完整清单基线**
  - 将固定十提交批次改为可选执行分组，不降低逐提交审计、完整集合校验和全局门禁。
  - 通过 S-Forge Kernel 更新并回读发布块，同步仓库镜像和双方哈希。
  - 生成并验证完整 DAG 清单，当前已从 440 条确定性扩展为 546 条；自动记录父、作者、日期、主题、路径、merge/revert 类型、patch ID 和本地 trailer 映射。
- [-] **Phase 2：逐提交行为契约与语义迁移**
  - 已闭合早期拓扑序 4、5、6、9 及引用块删除/剪切 series 的四项上游行为；其余 536 项保持显式 `pending`。
  - 按依赖和领域 series 执行，不以固定数量切分，不跳过早期未完成项。
  - [x] `2026-07-30`：`13396` 后端引用检查。`kernel/model/block.go`、`kernel/sql/block_ref_query.go`、`kernel/sql/database.go`、`kernel/treenode/blocktree.go`、`kernel/api/block.go`、`kernel/api/router.go` 已实现容器/文档/笔记本删除集、外部引用排除、加密索引扫描、索引迁移和严格参数校验；`go test -tags fts5 ./sql ./treenode ./model ./api -count=1` 通过。
  - [x] `2026-07-30`：`025c6fe...`、`c9a93d5...`、`9bfd981...`、`9e8db0d...` 引用块删除/剪切行为完成候选迁移。Kernel 提交 `bb15f54aa...` 与前端事务提交 `2ef29e1fd...` 保留完整来源 trailers；版本化行为决策将四项标记为 `verified`。`pnpm exec vitest --run` 的 5 个文件、31 项断言通过，拒绝引用确认或富剪贴板写入时均保持 DOM 不变。
- [-] **Phase 3：滚动验证与持续交付**
  - 发布规程改为 `D1...Dn` 小步集成，主线只在每次 delivery 的最终检查和 merge 期间进入短时窗口。
  - 首个 delivery 包含已验证的搜索排序、数据库属性标题、筛选选项和清单工具；`D001=b84bd2daa1f6f270eed167a2ab22f97c24fab595` 已接入主线。活动 Kernel `39cfb62bc359...`、主线 `1c97fd45af9f...` 的空后端差异、完成态 gate 与六入口 HTTP 200 证据已将状态闭合为 `integrated`。
  - 运行时门禁新增提交自动执行与 crash incident 硬约束；关联 `docs/ttt/AI模块改进/Forge模式后端热重启与版本回退.ttt.md` 重新打开实现和真实运行验收。
  - 后续每个 series 从最新主线建立分支，主线漂移时先同步并重跑受影响测试，不使用旧架构继续累积。
  - [ ] 当前 Issue `8554` / `13396` series 在 Git 集成、运行时 gate 和六入口验证闭合后，为 Codex 配置 S-Forge MCP；配置前核对本机 MCP 入口与同源认证链路，配置后通过一次只读健康调用验证，不在未完成 series 中途修改 Codex 环境。
- [ ] **Phase 4：完整范围验证**
  - Kernel：`go test -short -tags fts5 ./...`、`go vet -tags fts5 ./...`。
  - App：`pnpm typecheck`、`pnpm test`、`pnpm test:browser`、`pnpm build`。
  - 所有定向、数据迁移和运行验证结果写入 `verification.json`。
- [ ] **Phase 5：拓扑闭合**
  - 以完成全部滚动交付且通过完整门禁的当前主线固定 `P`，创建唯一 `M`，验证双亲、树和祖先关系。
  - 主线以 `--ff-only` 接纳已经具备双亲的 `M`，再次回归并归档；若主线移动则重新同步和验证。

## 当前已闭合上游项

| 拓扑序 | 上游提交 | 处置 | 本地提交 | 验证 |
|---:|---|---|---|---|
| 4 | `5437866299dba896b41b356abf4d28f059c9d89c` | `ported-exact` | `32112e1e95f0f76e94f1783da5f5e4a1e91ce04a` | `go test ./model -count=1`；patch ID `02e40f1a...` 相同 |
| 5 | `b8543f5b959a94b830db45853751a52222a3df2b` | `ported-semantic` | `954a664eb6ca2237646e2ea4a422c7805f944dd7` | AV 19 文件、46 测试；桌面/移动 SCSS 编译 |
| 6 | `446be135969fc9676fd810968ba2f646a2ad9d55` | `ported-semantic` | `ca678858d343f5c1cc5bb8a9b95827cf402ab296` | 最终 DOM 契约测试；组合生产 patch ID `2675b7b0...` |
| 9 | `30e2e49de7f4539f7939d645bf7905ad38c6f69b` | `ported-semantic` | `ca678858d343f5c1cc5bb8a9b95827cf402ab296` | AV 19 文件、46 测试；桌面/移动 SCSS 编译 |

## 滚动交付记录

| Delivery | 主线基线 | Series 头 | 集成提交 | 状态与验证 |
|---|---|---|---|---|
| `D001` | `7845c47e3620a7cd411648e36fd508c43ea757f7` | `07932cf8088c7cb1ec5e315d00568470e55380a0` | `b84bd2daa1f6f270eed167a2ab22f97c24fab595` | `integrated`；代码门禁、Kernel 热切换、前端当前性及六入口健康通过 |
| `D002` | `bf304048cf6fee55c891f49f5482e3ab44a59acd` | `659e223c3c4045ecfbf1537542e1a4a8fb36b61c` | 待创建 | `candidate-verified`；审计清单 544/41/8 mapped，工具 6/6、Protyle 31/31、`sql/treenode/model/api` 定向回归通过；主线当前 Kernel 正等待受保护测试人工审批，未在旧运行实例上创建集成窗口；完整类型检查仍有既有阻塞，未伪报为全局通过 |

## 验收标准

- [x] 主仓库工作树和索引未被旁路执行改变。
- [x] `L0/U0/U1` 固定且祖先关系成立。
- [x] 发布规程与仓库镜像已同步修订，固定十提交门槛归零。
- [x] `commits.jsonl` 与当前 `U0..U1` 完整 DAG 双向一致：544 条、无重复、无范围外 SHA。
- [ ] 最终 tip 刷新后的全部项处于允许终态，且本地映射、行为契约、代码和测试证据完整。
- [ ] 所有已验证 series 均通过 `D_i` 持续进入主线，`deliveries.jsonl` 与实际父关系一致，主线在整轮审计期间保持可继续开发。
- [ ] 每个 delivery 均有常驻 S-Forge 前端当前性、Kernel 新鲜度、健康端点和关键页面证据；下一 series 不在旧运行实例上推进。
- [ ] 上游全部行为与 S-Forge 本地 Agent/MAGI、Forge、Protyle、AppFacade、CaliburRouter 等改进同时保留。
- [ ] `cycle.json` 规定的完整门禁全部通过，范围未为通过验收而收窄。
- [ ] `M` 已创建并通过父、树、祖先和主线运行验证；每个业务变更由对应 `D_i` 提供可回退边界。

## 风险

- 505 项包含跨提交 series、merge 冲突解决和可能的后续取代；只移植早期提交会形成不完整中间态。
- Protyle、Agent、AppFacade 和 Kernel 已在本地大规模拆分，上游路径与真实领域所有者存在一对多映射风险。
- 多语言、锁文件和生成物必须使用现有生成链重建，不能把上游文本补丁直接当作最终产物。
- 上游 `dev` 仍可能移动；每个新 series 建立前和最终闭合前重新获取。series 内固定完整 SHA，边界处快进扩展同一清单；不以旧快照继续反复修改已被后续提交取代的行为。
- 主线可在 series 实施期间继续前移；每次交付前必须同步最新主线并重跑受影响验证，禁止以文本冲突数量代替意图复核。
- Git 主线与本地运行实例可能短暂分离；该窗口只允许用于受控构建和切换，delivery 保持 `git-integrated` 且不得启动下一 series。

## 已归档/已完成

- **2026-07-29：主线原子收口。** 主线 `multipleAI` 在 `7845c47e3` 清洁；保留 `backup/atomic-closeout-20260729-before-rewrite` 回退点。
- **2026-07-29：首批三个行为提交。** 搜索排序、数据库属性标题和筛选选项分别形成原子提交；所有来源 trailers 经重写后可由 `git interpret-trailers` 解析，重写前后业务 tree 相同。
- **2026-07-29：上游 tip 刷新。** 官方 `dev` 从 `c36a50cf...` 前移到 `28e38647...`，范围由 413 扩展到 440；候选分支与 trailer 审计路径同步更新。
- **2026-07-29：规程修订。** 用户取消固定十提交要求；发布块和仓库镜像改为可选执行分组，完整 DAG 仍是唯一闭合边界。
- **2026-07-29：清单工具。** 隔离 Git 测试覆盖分支、merge、revert、patch ID、本地 trailer 映射、人工审计字段保留、滚动交付配置保留、受控刷新和确定性重建，共 4/4 通过。
- **2026-07-29：显式失败记录。** 首次生成缺少 `candidate-head`，第二次缺少 `candidate-created-at`，第三次使用了错误展开的候选 SHA；三次均立即失败且未产生伪成功。补齐真实 `ddc21df46e3edf597c714318a1bfc5340ec4dcba` 后生成 440 条并验证通过。
- **2026-07-29：交付模型纠正。** 识别出“隔离仓库完成全部 440 项后再一次性合入”会长期冻结主线并阻塞新功能；规程改为每个已验证 series 形成独立 `D_i`，最终 `M` 仅承担完整上游祖先边界闭合。发布块通过正式 Block API 更新并回读，7 项新旧语义断言通过。
- **2026-07-29：首个滚动交付进入 Git。** `D001=b84bd2daa1f6f270eed167a2ab22f97c24fab595` 以 `--no-ff` 接入 `multipleAI`；第一父为原主线，第二父为已验证 series 头，树与第二父一致。交付前后代码回归均通过，三项执行失败及确定性处置已写入 `deliveries.jsonl`；常驻运行实例更新完成前不归档为 `integrated`。
- **2026-07-29：运行时提交门禁实现。** 版本化 hooks、持久 gate 状态、前端更新、Kernel 热切换、多入口探针和 crash/回退 incident 已实现；`pnpm forge` 新增先于任何启动副作用的全仓库清洁门禁。前端-only 新鲜度改由成功 gate commit 跟踪，不再错误绑定 Kernel revision；一次性开发构建与常驻 watcher 复用同一 Webpack 配置。定向测试 40/40、最新完整 Node 239 项、Vitest 845 项通过，11 个开发目标一次性构建通过，六个关键页面均返回 200。完整类型检查仍失败并保持显式记录；该批处于引导提交前，`D001` 保持 `git-integrated`。
- **2026-07-29：发布规程再次同步。** 新增具名 Block 发布/回读工具，确认原始 Kramdown 仅因 IAL 属性顺序不稳定而产生不同字节哈希；改用 LF 换行与 IAL 属性字典序规范化，连续两次回读哈希一致，10 项语义断言通过。发布块更新时间、规范化哈希与镜像哈希已同步到 `cycle.json`。
- **2026-07-29：重启保护策略去重。** Node Supervisor 与 Go Agent 工具改为严格读取同一 `kernel/forge_restart_test_policy.json` schema 2；重复硬编码路径表归零，策略文件和所有 Kernel 测试仍由不可移除的代码根规则保护。运行版本持久化策略快照，候选预检拒绝相对活动版本缩窄保护范围；缺失、空集合、重复、乱序、越界、未知字段和全包测试命令收窄均明确失败。组合 Node `45/45` 与 `go test ./mcp/tools -count=1` 通过；该批尚未形成引导提交，D001 继续保持 `git-integrated`。
- **2026-07-29：引导批次全量回归进行中。** Node `244/244`、Vitest `845/845` 与 Kernel 全包 vet 通过；Kernel 全包短测试在 API 任务目录远程 guardian 绑定契约上失败，具体为生产路径先返回“仅本机 WebUI”而未进入 guardian 授权。该失败不通过缩小 `./...` 范围或修改测试预期隐藏，D001 状态不变。
- **2026-07-30：D001 运行时闭合。** Forge 连续性协议与提交 gate 已部署；Kernel job `2026-07-30T00-36-01.224Z-a1576038` 通过完整核心门禁并晋升 `39cfb62bc359...`。主线 `1c97fd45af9f...` 的完成态 gate 证明后续无 Kernel/前端运行时差异，Supervisor、Kernel 与六个页面探针通过，D001 标记 `integrated`。
- **2026-07-30：上游最新 tip 增量扩展。** 官方 `dev` 从 `28e38647...` 快进至 `ca1ffce3...`，增加 27 个提交与 2 个 merge，完整范围为 467/35。受测工具保留 4 个既有 `verified` 审计，463 项保持显式 `pending`；非快进更新会明确失败。发布规程同步增加每个 series 及最终闭合前刷新要求，11 项发布语义断言和连续 Kramdown 哈希校验通过。
- **2026-07-30：滚动刷新工具接入主线。** 支撑 series `491705bbdf76...` 通过双亲 merge `71d489fd5d17...` 接入；提交 gate 记录无 Kernel/前端运行时差异，Supervisor、Kernel 和六入口探针通过。首次主线清单复核因尚未获取 `ca1ffce3...` 对象明确失败；获取官方 `dev` 后 467 项复核通过，失败与处置同步写入 `verification.json`。
- **2026-07-30：第二次 tip 快进。** 获取缺失对象时官方 `dev` 已继续前移至 `2e68bda1...`；旧 tip 是其祖先，新增 9 个普通提交，范围扩展为 476/35。清单自动校验通过，4 个既有 `verified` 审计保持不变，472 项显式 `pending`。
- **2026-07-30：Issue 8554 series 建立前再次刷新。** 官方 `dev` 从 `2e68bda1...` 先快进至 `72987919...`（新增 3 个普通提交），再快进至 `abc1a85a...`（新增 Issue 18435 提交及 1 个 merge）；当前完整范围为 481/36。两次均由 `advance-tip` 校验完整 DAG 后成功，4 个既有 `verified` 保持不变，477 项显式 `pending`；新增提交未取代 Issue 8554，但已纳入本 series 的全局基准。
- **2026-07-30：最新 tip 刷新与大二进制审计修复。** 官方 `dev` 快进至 `bdd005b4a...`，新增 Pandoc 初始化修复、Issue `13396` 引用块剪切/删除检查 `9e8db0d...` 及 Pandoc 升级，完整范围为 484/36。原审计工具将 `git show --binary` 输出置于 128 MiB 内存缓冲，在 Pandoc 压缩包 patch（约 134 MiB）上确定性触发 `ENOBUFS`；改为写入唯一临时补丁文件再由 `git patch-id --stable` 读取，保持 patch-id 算法与二进制内容完整参与。工具 5/5 测试通过，真实 `advance-tip` 完成并保留 4 个既有 `verified`，其余 480 项显式 `pending`。Issue `13396` 已成为当前跨块删除 series 的直接后续依赖，而非可忽略的平行修正。
- **2026-07-30：Issue 13396 后端引用完整性闭合。** 以 `9e8db0d...` 的最终语义实现删除集合外引用检测：普通与已打开加密索引均通过参数化批查询，删除集内的引用源按块和文档根排除；容器选择递归展开后代，文档与笔记本删除按完整根集合检查。已有数据库和加密数据库新增幂等索引迁移，API 拒绝错误类型、空数组、非法 ID 和非子集 `exactIDs`，缺失块明确返回错误。新增/更新 `kernel/sql/encrypted_query_test.go`、`kernel/model/block_ref_check_test.go`、`kernel/treenode/blocktree_query_test.go`、`kernel/api/block_ref_test.go`；`go test -tags fts5 ./sql ./treenode ./model ./api -count=1` 全部通过。
- **2026-07-30：引用块删除/剪切候选闭合。** 以独立版本化决策文件记录四个上游提交的前置条件、状态转换、输出、不变量、失败路径、代码证据和测试证据；审计工具新增 `apply-decisions`，只更新人工审计字段而不改写 Git trailer 映射，隔离仓库测试 6/6 通过。候选 `bb15f54aa...`、`2ef29e1fd...` 的跨块删除与剪切事务通过 5 文件、31 项前端断言；`sql`、`treenode`、`model`、`api` 定向 Kernel 回归均通过。完整 `pnpm typecheck` 的既有严格诊断保持显式记录，未作为通过证据。
- **2026-07-30：交付前最新 tip 刷新。** 通过本机 `127.0.0.1:7890` 代理获取官方 `dev`，确认 `bdd005b4...` 是 `6d23075f...` 的祖先；新增 21 个提交、1 个 merge，涉及 AI 模型上下文、移动 Tab、动态引用和搜索替换，不取代本 series 的引用块删除/剪切行为。`advance-tip` 完整重建并验证 505/37 清单，8 个 `verified` 审计均保留，497 项保持显式 `pending`。
- **2026-07-30：最新上游 tip 与增量审计优化。** 官方 `dev` 从 `6d23075f...` 快进至 `66c7c6e8...`，新增 39 个提交、4 个 merge，涉及 AI 上下文、表格编辑、富文本剪贴板、移动导航、导出覆盖和 Lute/Go 依赖；未发现其取代 Issue `8554` / `13396` 的删除与剪切语义。审计工具改为对同一不可变 SHA 复用已提交清单中的稳定 patch ID，仍重新生成路径、元数据、依赖关系和新增记录，避免每次 tip 快进重复计算整个冻结范围。工具回归 6/6 通过，`advance-tip` 校验并扩展清单到 544/41，8 个既有 `verified` 保持不变，536 项显式 `pending`。
- **2026-07-30：最新上游 tip 再次刷新。** 官方 `dev` 从 `66c7c6e8...` 快进至 `0fd89fde...`，新增两个普通提交：大型 CSV 写入数据库的粘贴性能/正确性处理（Issue `10767`）与 Dialog 拖拽不覆盖标题栏（Issue `18455`）。`advance-tip` 与随后 `verify` 均通过，完整清单为 546/41；8 个既有 `verified` 审计保持不变，538 项显式 `pending`。这两个新行为未取代当前 Issue `8554` / `13396` 引用块删除与剪切 series，已纳入后续独立审计。
- **2026-07-30：D002 当前主线重验证。** 候选将 `multipleAI=bf304048...` 以真实 merge 合入，冲突仅位于 TTT；保留主线的提交验证/部署解耦语义和候选的 `U1=66c7c6e8...` 审计边界。清单 `refresh/verify` 绑定候选 `659e223c...` 并通过 544/41/8 映射校验；`go test -tags fts5 ./sql ./treenode ./model ./api -count=1` 全部通过，Protyle 引用块删除/剪切契约测试 5 文件、31 断言通过。当前 Kernel 热切换仍需受保护测试人工批准，未把候选合入或标记为 `git-integrated`。
