# packages-vectordb文件与测试组织重构

## 父任务目标

改进 `packages/vectordb` 包内部的生产文件与测试组织，使职责边界、测试分类和辅助设施符合已批准规程与仓库约束，同时保持公共行为、构建集合和测试意图兼容。

## 范围

- 治理前置：确认适用规程并建立任务跟踪。
- 客观调查：在规程获批后调查文件树、Go模块元数据、生产代码、测试、构建标签、测试辅助设施与外部引用。
- 后续实施：仅在调查完成且实施方案获得父任务确认后开展文件与测试组织调整。
- 当前子任务禁止修改任何Go生产代码或测试代码。

## 适用规程

- 已获用户批准：`docs/规程/后端开发/Go包内部文件与测试组织重构.procedure.md`（文件内状态元数据仍为“草案，待批准”，本任务以用户明确批准指令为准）。
- 测试组织补充参考：`docs/规程/测试与修复/后端Go测试编写.procedure.md`，其适用范围限定 `kernel`，不能单独作为本任务完整规程。
- 不适用：`docs/规程/后端开发/Go模块实现.procedure.md` 明确面向新功能实现，并排除大规模代码重构。
- 文档约束：`docs/规程/文档管理/文档分类.procedure.md`、`docs/规程/文档管理/文档命名.procedure.md`。

## 阶段清单

- [x] 检查 `docs` 下现有规程的适用范围。
- [x] 创建专注于Go包内部文件与测试组织重构的规程草案。
- [x] 用户已批准专用规程。
- [-] 完整调查 `packages/vectordb` 的文件树、模块元数据、生产代码、测试、构建标签、辅助设施与外部引用。
- [ ] 将客观调查结论、风险与组织建议写入本文档。
- [ ] 确认修改复杂度；若属于复杂修改，在任何代码变更前备份全部拟修改原始文件。
- [ ] 依据获批规程实施重构并完成适用验证。

## 本子任务状态

- 状态：调研进行中
- 已完成：现有规程适用性检查、专用规程草案创建、用户批准确认、任务跟踪文档建立。
- 当前工作：完整调查 `packages/vectordb` 的文件树、模块元数据、生产代码、测试、构建标签、辅助设施与外部引用。
- 调查边界：本子任务只读调查实现文件，仅允许更新本文档，不修改Go生产代码、测试代码、模块文件或其他实现文件。
- 备份要求：后续调查确认属于复杂修改时，任何Go生产代码或测试代码变更前必须备份全部拟修改原始文件。

## 验收标准

- 规程草案不超过300行，只约束客观成果，且覆盖包内生产文件、测试分类、辅助设施、构建标签、外部引用与兼容性。
- 本文档记录父任务目标、范围、阶段状态、验收标准及失败记录。
- 规程未批准期间不读取或修改 `packages/vectordb` 的Go生产代码和测试代码。
- 后续调查结论直接沉淀于本文档，不通过子任务完成摘要中转大量内容。
- 后续实施明确引用获批规程与本文档，并在复杂修改前完成原始文件备份。

## 失败记录

- 暂无工具失败。
