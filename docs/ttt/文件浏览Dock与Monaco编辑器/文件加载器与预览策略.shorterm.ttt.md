# 文件加载器与预览策略（TikTocTak）

> **归属**：[`文件浏览Dock前端.shorterm.ttt.md`](文件浏览Dock前端.shorterm.ttt.md)
> **范围**：把 SACAssetsManager 最终内置加载器序列映射到 S-Forge assets 领域；卡片缩略图和主要预览页签共享同一格式策略。

## 参考实现冻结

- `94c8534a` 的最终内置缩略图序列为 `svg`、`sharp`、`sy`、`d5m`、Windows `systermThumbnailWin64`。
- `pdf`、`eagle`、`onlyName` 属于历史生成器，不能误记为最终注册 Provider；系统 Provider 失败时不得把文件图标伪装成原图缩略图。
- `svg` 返回 SVG 原始字节；`d5m` 读取压缩包内 `icon.png`；栅格图片由纯 Go 图像 Provider 处理；Windows 系统 Provider 处理已安装 Shell 扩展支持的格式。

## 实施状态

- [x] 清理仓库根目录误留的 Go 编译缓存 `.tmp-gocache`（约 9,854 个文件、1.05 GB）；当前目录不存在。根 `.gitignore` 中对应规则仍是未提交的工作区改动，来源不是 Git 历史中的编译脚本。
- [x] 建立 `kernel/assets` 格式策略，供文件属性/预览分类和缩略图 Provider 共用。
- [x] 增加 SVG 原图 Provider 和 D5M `icon.png` Provider，按参考优先级注册并补真实 fixture。
- [x] 增加 SiYuan `.sy` 专用 Provider：对照 `source/server/processors/thumbnail/internalGeneraters/sy.js`，读取运行时 `stage/icon.png` 的真实字节；图标缺失或空文件显式报错，不把通用文件图标当作缩略图。
- [x] 建立前端 `app/src/asset/assetFormat.ts` 格式策略；AssetCard、画廊表格行、旧资源预览和文件主要页签均通过同一格式矩阵判断。
- [x] 保持原图与缩略图职责分离：主预览使用已授权内容 URL，卡片只请求缩略图 URL；失败显式报错，不生成占位回退。

## 验收证据

- Go：Provider `CanHandle`、真实 SVG 字节/MIME、真实 D5M 压缩包图标和缓存命中测试。
- 前端：格式策略矩阵、卡片与文件页签共享策略、SVG/D5M 请求链路和错误状态测试。
- 构建：目标 Vitest 与 `go test`；不把挂载测试当作真实桌面预览验收。

## 2026-08-11 `.sy` Provider 切片

- 参考证据：`SACAssetsManager@94c8534a` 的 `source/server/processors/thumbnail/loaders/internal.js` 注册序列为 `svg`、`sharp`、`sy`、`d5m`、`systermThumbnailWin64`；`internalGeneraters/sy.js` 对 `.sy` 返回产品 `icon.png`。
- S-Forge：新增 `kernel/thumbnail/provider_sy.go`，由 `util.WorkingDir/stage/icon.png` 解析产品图标；`Manager` 注册顺序为 `SVG -> SY -> D5M -> Windows -> GoImaging`。
- 边界：`.sy.zip` 不匹配；图标路径为空、读取失败和空字节均返回显式错误；Provider 不读取或改写 `.sy` 源文件。
- 证据：追加缺失图标边界前 `go test ./thumbnail -count=1`（通过），`go test ./assets -count=1`（通过）；真实 PNG fixture 验证返回字节与 `image/png` MIME；追加边界后再次运行被系统 `go-build` 缓存 `Access is denied` 阻断，未将环境阻断记作代码失败；`git diff --check -- kernel/thumbnail/provider_sy.go kernel/thumbnail/provider_sy_test.go kernel/thumbnail/manager.go`（通过）。
- 保持未完成：SVG/D5M 的完整真实 fixture、Windows Shell provider 现场能力、前端主预览/卡片共享策略和完整 SAC loader 矩阵仍由本 TTT 追踪，不以本切片宣称全部加载器已迁移。

## 2026-08-11 前端格式策略收口

- `renderAssetsPreview` 和 `genAssetHTML` 删除本地重复扩展名集合，改用 `getAssetFormat`；`.heic`、`.opus`、`.3gp` 和 `.tsx` 等参考矩阵中的格式不再因旧入口漏判而落入错误分支。
- 资产卡片与画廊表格行继续直接消费 `assetFormat`；`.sy` 与 `.d5m` 均标记为真实 Provider 生成缩略图，文件主要页签继续使用后端 `assets.Classify` 结果；没有增加第二套 loader 或兼容转发。
- 证据：`pnpm exec vitest --run test/asset/renderAssetsPreview.interaction.test.ts --reporter=verbose`（3 个用例通过）；`pnpm exec vitest --run test/asset --reporter=dot`（8 个文件、27 个用例通过）；`pnpm run typecheck:protyle-contract`（通过）。

## 2026-08-11 SVG/D5M Provider 真实夹具闭环（已完成切片）

- [x] 对照 `SACAssetsManager@94c8534a` 的最终 loader 序列，补充真实临时 SVG 文件和真实 ZIP/D5M 文件夹具；SVG 返回原始字节，D5M 只读取根级 `icon.png`，嵌套同名文件不会被误选。
- [x] Provider 边界覆盖大小写扩展名、`.svg.zip`/`.sy.zip` 不匹配、缺失 D5M 根图标和原始字节 MIME（`image/svg+xml`/`image/png`）；Manager 顺序验证为 `SVG < SY < D5M < GoImaging`，Windows provider 仍位于专用 provider 之后。
- [x] 画廊旧资源预览的 SVG、D5M、SY 路径均通过共享 `/api/s-forge/thumbnail` 入口；前端测试不生成第二套 URL 或图标回退。
- [x] 证据：`go test ./thumbnail -count=1`（通过，含 4 个新增业务夹具用例）；`pnpm exec vitest --run test/asset/renderAssetsPreview.interaction.test.ts test/asset/AssetCard.interaction.test.ts --reporter=dot`（2 个文件、7 个用例通过）。
- [ ] Windows Shell provider 的真实系统扩展能力和完整桌面原图/卡片现场验收仍待单独里程碑；本切片不把专项夹具测试扩大成整个 loader 矩阵完成。
