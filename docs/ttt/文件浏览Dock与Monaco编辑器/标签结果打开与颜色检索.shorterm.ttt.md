# 标签结果打开与颜色检索（TikTocTak）

> **归属**：[`文件属性Dock.shorterm.ttt.md`](文件属性Dock.shorterm.ttt.md) 和主 TTT
> **范围**：把已冻结的标签定义/索引能力接入文件浏览领域，交付标签结果打开、标签树和 RGB/HSL/调色板检索的可测试切片；不把查询、导航和渲染塞进一个万能控制器，也不扩展到 Monaco 编辑器。

## 参考审计

| 来源 | 最终证据 | 可复用语义 | S-Forge 修正 |
| --- | --- | --- | --- |
| SACAssetsManager | `94c8534a:source/UI/components/tags/tagTree.vue`、`tagItem.vue` | 标签树刷新、层级显示、计数、删除、拖放、点击打开资源结果；Ctrl 点击打开笔记搜索 | 通过声明式导航端口发出标签名/root 查询，不保存绝对路径资产数组 |
| SACAssetsManager | `94c8534a:source/data/tags.js` | 标签与资源合并、移除标签标记、按文件找标签 | 使用 `assetmeta` 唯一主数据和 SQLite 查询；保留异步刷新竞态抑制 |
| S-Forge | `kernel/assetmeta/index.go:580-840` | `SearchRequest`、标签任意/全部匹配、RGB 容差、HSL 环形 Hue、调色板比例、分页/排序/跨根 | 增加文件浏览仓储/守卫/响应模型，UI 不直接拼请求或索引 SQL |

## 冻结契约

- 查询请求只携带 `rootIDs`、`allRoots`、`keyword`、`tags`、`matchAllTags`、分页/排序和 `palette` 条件；颜色条件明确为 RGB 容差、HSL 范围（Hue 支持环形区间）或最低色块比例。
- 查询结果保持 `{rootID, path}` 地址和后端真实 `AssetMeta` 投影；结果打开端口接收不可变查询描述，不接触绝对路径。
- 标签树节点为 `{name, count, color, foreground, children, removed}`；树状态由一个控制器管理刷新修订、展开状态和删除确认，结果导航由注入端口执行。
- 结果查询、标签树刷新、颜色过滤和打开动作各自有 owner；仓储只做网络/响应守卫，展示层只做模型投影。
- 空结果、索引未就绪、部分元数据缺失、过期响应和后端错误必须是显式状态；占位按钮不计作完成。

## 实施清单

- [x] 为高级元数据搜索建立文件浏览仓储、请求/响应守卫和错误映射，覆盖标签/颜色/分页/跨根。
- [x] 实现标签树模型与刷新控制器，支持稳定排序、层级、计数、颜色和过期响应抑制；删除/拖放写入仍属于后续 M3。
- [x] 实现结果查询状态控制器和可注入“打开标签结果”端口；笔记搜索端口仍未接入。
- [x] 将标签树注册为独立 `sforge-file-tags` Dock，标签结果通过共享 `sforge-file-gallery` 页签打开；颜色检索表单调用后端索引，不做仅前端过滤。
- [x] 补后端边界测试（RGB 容差、HSL 跨 0、全部/任意标签、分页/跨根）和前端控制器/组件交互测试。
- [x] 通过目标类型检查、P0 lint、聚焦 Vitest/Go 测试后回写父 TTT；仍保留本短期文档，完成整体范围后移动到 `docs/ttt/archive/`。

## 当前缺口

- 标签结果页当前通过通用 `sforge-file-gallery` Tab 打开，笔记搜索/Ctrl 点击语义尚未接入。
- 当前标签 Dock 已从任务文件树移出，独立拥有标题、刷新、折叠和完整高度；SAC 的标签删除、拖放资源归属和 Ctrl 点击笔记搜索仍未接入。
- 外部根图片的调色板提取尚未接入索引生命周期；桌面真实根媒体命中仍缺验收证据。
- SACAssetsManager 的拖放绝对路径语义不能直接移植；拖放写入属于 M3 文件操作切片，本切片仅保留树节点导航事件。

## 2026-08-07 目录画廊筛选状态回归

- [x] 复现截图：目录 `D:\dev\.artifacts\passive-income\guide-render` 只有 PNG 时，画廊触发器显示 `.bmp`，并残留 `dev` 关键词，结果为空。
- [x] 根因证据：`SACAssetsManager-ref@94c8534a` 的 `useExtensions` 以空数组初始化，空数组在 `构建遍历参数` 中省略 `extensions`；S-Forge 原生 `multiple + appearance: base-select` 在 Windows Chromium 空选中时会把第一项 `.bmp` 绘制成按钮文本，造成“存在筛选条件”的错误视觉；目录切换前的关键词则来自 `FileBrowserSearchPanel` 未随 `scope.path` 重置。
- [x] 修正：新增 `FileBrowserMultiSelect.vue`，空值显示“扩展名”占位、支持搜索/全选/逐项勾选；目录画廊只接受显式全根查询页签的 `file.query`，路径切换重置关键词、扩展名、标签和颜色状态；空扩展名不写入 `FileBrowserSearchRequest.exts`。
- [x] 证据：`pnpm exec vitest --run test/sforge/fileBrowser`，21 个测试文件、55 个用例通过；新增真实挂载回归覆盖旧 `dev/.bmp`、面包屑切换后的空表单与空请求。
- [ ] 桌面真实窗口截图复验仍待 dev server/桌面运行链；本次未扩大到既有 P1+ lint 债务。

## 2026-08-07 空筛选第二轮回归

- [x] 目录页签范围判定增加空路径约束：带路径页签忽略布局恢复出的旧 `file.query`，因此不会把历史 `.tmp`、关键词或标签重新注入目录查询。
- [x] 全根标签结果点击清空后保留 `allRoots` 范围，但不恢复任何已清掉的 `tags`、`exts` 或颜色条件；表单的扩展名控件回到“扩展名”占位状态。
- [x] 后端 `assetmeta.SearchAssetsAdvanced` 增加 `Exts:nil` 与 `Exts:[]` 的无约束回归，确认空扩展名不会生成 SQL 类型条件。
- [x] 证据：`pnpm exec vitest --run test/sforge/fileBrowser`，21 个文件、55 个用例；`go test ./assetmeta ./filequery ./api -count=1`；`git diff --check`。
- [x] 全仓 `vue-tsc` 仍有既有错误；本轮目标文件筛选诊断为 0，未把全仓存量问题归因于本改动。

## 2026-08-08 空筛选生命周期与默认枚举证据

- [x] 前端目标文件类型诊断为 0；清空全根结果后 `allRoots` 保留，`tags`、`exts`、颜色和关键词均不恢复，刷新仍发送无筛选请求。
- [x] `filequery` 接入 `filebrowser.ScanContext` 窄枚举端口；空元数据查询覆盖未索引文件，标签/调色板/尺寸/星级门槛仍保持 `assetmeta` 索引语义。
- [x] `go test ./assetmeta ./filequery ./api -count=1`、`pnpm exec vitest --run test/sforge/fileBrowser`（21 个文件、55 个用例）和 `git diff --check` 通过。
- [ ] 本短期文档仍保留，待标签删除/拖放、桌面真实窗口和大规模媒体验收完成后整体移动到 `docs/ttt/archive/`，不删除。

## 2026-08-08 空筛选持久化与未索引文件回归（已完成切片）

- [x] 当前页签查询状态独立于布局恢复时的初始 `file.query`；清空后刷新、目录切换和组件重建都不恢复旧的 `.tmp`、标签或关键词。
- [x] 空筛选请求不发送 `exts: []` 或历史 `exts`；全根结果只保留 `allRoots: true` 这个范围状态。
- [x] 无标签、颜色、尺寸和星级门槛的默认查询由授权 `filebrowser` 遍历提供文件集合，再叠加名称/路径/扩展名/目录范围筛选；未进入 `assetmeta` 索引的文件返回基础投影。
- [x] 标签、调色板和元数据门槛查询继续使用 `assetmeta` 索引，没有用默认枚举替代元数据语义。
- [x] 小规模验证覆盖空请求、`.tmp` 清空后刷新、未索引文件、扩展名/目录范围、授权根和取消；大规模目录验收仍在交互验收 TTT 追踪。

## 2026-08-08 旧目录筛选迁移回归（已完成切片）

- [x] 带路径的目录画廊页签现在只持久化 `{rootID, path, name}`；启动时迁移并移除旧版本写入的 `file.query`，因此历史 `.tmp`、关键词、标签和颜色条件不会再次进入目录范围。
- [x] 空查询统一去除空字符串和空数组；全根页签提交空表单时保留 `allRoots` 范围，清空筛选后布局重建仍显示“扩展名”占位。
- [x] 真实 Vue 挂载回归增加旧目录 query 清理断言；此前的目录切换、全根清空、刷新和重建用例继续通过。
- [x] 证据：`pnpm exec vitest --run test/sforge/fileBrowser/FileBrowserGalleryTab.interaction.test.ts test/sforge/fileBrowser/FileBrowserSearchPanel.interaction.test.ts`，2 个文件、7 个用例通过；目标文件类型诊断无新增输出；`git diff --check` 通过。

## 2026-08-08 全根查询状态归属修正（已完成切片）

- [x] 标签结果页签标记为 `scope: "global"`，清空或替换标签/扩展名后仍保持全根结果身份，布局恢复读取最新查询。
- [x] 空筛选只保留 `allRoots` 和排序；当前结果不包含历史 `.tmp` 时，扩展名仍可在菜单中取消。
- [x] 证据：标签 Dock、画廊页签和搜索表单的 11 个真实挂载用例通过；标签删除、拖放和 Ctrl 笔记搜索仍属于后续缺口。

## 2026-08-08 标签来源与运行期筛选回归

- [x] 标签结果页的标签条件作为打开入口来源保留，但关键词、扩展名和颜色等表单筛选只存在于画廊组件运行期；清空不会把 `.tmp` 或空 `exts` 写入布局数据。
- [x] 真实挂载回归确认：旧全根 query 带 `.tmp` 时首次请求不携带该扩展名；清空/提交空表单后刷新和重建仍为空，标签来源仍可按入口恢复。
- [x] 当前完整文件浏览测试为 21 个文件、60 个用例；目标类型检查和 `typecheck:protyle-contract` 通过。
