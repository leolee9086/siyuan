# 文件浏览树与瀑布流页签（TikTocTak）

> **状态**：进行中
> **归属**：[`文件浏览Dock前端.shorterm.ttt.md`](文件浏览Dock前端.shorterm.ttt.md)
> **范围**：把文件树 Dock 与资源查询/瀑布流结果页签按 SACAssetsManager 的最终结构接线；不把查询结果继续嵌入树 Dock。

## 参考行为冻结

- 左侧/侧边 Dock 只负责工作空间根、全部历史 Agent 绑定根、递归目录树、选择和目录导航。
- 双击根或目录打开独立资源结果页签；页签通过 `{rootID, path}` 接收范围，不携带绝对路径。
- 结果页签复用 s-forge 现有 `TabRegistry`、`VirtualMasonryGrid`、`AssetCard` 和文件属性/打开端口；查询筛选仍由 `FileBrowser.query` 领域仓储负责。
- 标签、星级、注释和调色板投影显示在卡片；颜色/标签查询调用后端索引，不做前端结果猜测。

## 实施清单

- [x] 移除树 Dock 内嵌结果列表，树只负责根和目录导航。
- [x] 注册独立 `sforge-file-gallery` 页签，支持 TabRegistry 创建/恢复和数据守卫。
- [x] 接入 `VirtualMasonryGrid`，提供图片缩略图、非图片图标、标签/颜色/星级和空/加载/错误状态。
- [x] 树节点、瀑布流卡片和属性 Dock 共享 `{rootID, path}` 选择及打开端口。
- [x] 补真实 DOM 组合测试：树目录双击创建瀑布流页签；真实瀑布流挂载验证范围查询、网格卡片投影、共享选择和双击打开。
- [x] M2 基础切片：接入扩展名筛选、颜色/RGB/HSL/调色板请求、卡片尺寸控制和属性列投影，并以真实挂载测试验证请求和显示结果。
- [x] 修正空扩展名筛选显示：参考项目空数组语义为“不限制扩展名”；S-Forge 改用可搜索/全选的自定义多选触发器，空值显示“扩展名”而不是 Windows `base-select` 错误显示的首项 `.bmp`。目录面包屑切换同时清空旧关键词、扩展名、标签和颜色状态。
- [x] 第二轮状态修正：带路径页签拒绝旧 `file.query`，全根结果清空时保留全根范围而不恢复扩展名；空 `Exts:nil/[]` 已由后端索引测试证明不生成类型筛选。
- [ ] 仍缺桌面 TabRegistry 布局恢复截图、真实根媒体加载和大结果滚动验收；本切片不把挂载测试等同于完整桌面验收。
- [ ] 仍缺参考画廊的表格模式、框选多选、拖放、批处理菜单和 Everything/AnyTXT 外部索引入口；面包屑、递归开关和子目录包含选择已落到目录列表与查询范围契约，扩展名过滤、属性列和颜色查询不能再记为缺失。

## 2026-08-08 空筛选生命周期回归（进行中）

- [x] 页签保存“当前查询”而非反复读取初始 `file.query`；清空后刷新/恢复不再重新注入旧扩展名（尤其 `.tmp`）。
- [x] 默认空筛选结果覆盖授权根中尚未建立 `assetmeta` 记录的普通文件；索引只负责补充元数据和执行标签/颜色/元数据门槛查询。
- [x] 聚焦测试已补齐：真实临时目录覆盖空 `exts`、`.tmp`、扩展名、关键词/范围、未索引文件、Agent 根、越权路径和取消；前端覆盖清空后刷新。
- [ ] 桌面真实 TabRegistry 恢复和大规模媒体加载仍不能由挂载测试替代。

## 2026-08-08 目录页签旧 query 清理（已完成切片）

- [x] 带路径的画廊页签不再接受布局恢复的历史 `file.query`；挂载时删除旧数据并以当前 root/path 作为唯一范围。
- [x] 空筛选请求不携带空 `exts`、空标签或空关键词；全根结果保留范围状态，目录结果保持无筛选默认枚举。
- [x] 真实挂载测试覆盖旧 `.tmp` query 的迁移清理，避免把“控件为空”和“布局数据仍带条件”混为一谈。

## 2026-08-08 当前查询来源与空扩展名入口（已完成切片）

- [x] 全根结果页签以 `scope: "global"` 保持页签范围身份；布局数据只保留根、标签、调色板等稳定来源，关键词和扩展名等运行期筛选不写回，因此重建不恢复旧 `.tmp`。
- [x] 目录结果页签以 `scope: "directory"` 只保存根和相对路径；路径切换通过稳定 key 重建筛选表单，关键词、扩展名、标签和颜色均回到空值。
- [x] 当前查询中的扩展名即使不在当前结果集合，也保留在扩展名菜单中，允许直接取消导致空查询。
- [x] 证据：`FileBrowserGalleryTab.interaction.test.ts`、`FileBrowserSearchPanel.interaction.test.ts` 和查询仓储回归共 14 个目标用例通过；完整 `pnpm exec vitest --run test/sforge/fileBrowser` 为 21 个文件、60 个用例；`pnpm run typecheck:protyle-contract`、目标 `vue-tsc` 和 `git diff --check` 通过。
- [ ] 桌面 TabRegistry 恢复截图、真实根媒体加载和大结果滚动仍未完成。

## 验收证据

- 代码：树 Dock 不再渲染查询结果列表；独立页签注册、恢复、销毁和开目录入口可追踪。
- 测试：`pnpm exec vitest --run test/sforge/fileBrowser`（21 个测试文件、60 个用例）覆盖独立页签注册、查询竞态、根范围/标签/颜色请求、网格卡片投影、目录范围切换和目录双击打开；本轮目标 `vue-tsc` 筛选诊断为 0。
- 残余：桌面截图、真实根媒体加载和大规模瀑布流滚动验收在交互验收 TTT 中继续登记。

## 2026-08-08 运行期筛选归属修正

- [x] 旧全根 `file.query.exts` 仅作为迁移输入清洗，首次查询直接使用空扩展名；运行期选择 `.png` 后刷新仍可使用当前组件状态，关闭重建则回到空筛选。
- [x] 标签入口的 `tags` 保持为稳定来源，清空动作只清空当前表单和查询请求，不把标签来源误删或与任务文件树混合。
- [x] 参考 `SACAssetsManager` 的 `useExtensions` 空数组语义，扩展名控件没有首项伪选；可选尺寸元数据缺省时由响应守卫填 `0`，未索引普通文件可以正常投影。

## 2026-08-08 响应错误定位与性能回归

- [x] 查询错误不再把包络错误和单项字段错误混成同一句：错误消息包含响应层级、条目索引、相对路径、字段、期望类型和实际类型。
- [x] 对附件中的真实本地目录响应做了 200 条解析验证：`totalCount=674`、`pageCount=4`，全部通过；前端守卫解析耗时约 `0.6-0.9ms`。
- [ ] 真实 D 盘全量扫描、跨根缓存失效和桌面大结果滚动仍属于 M1/M5 验收，不以该小规模基线替代。

## 2026-08-08 画廊顶栏产品化切片（进行中）

- [ ] 参考 Eagle/SACAssetsManager 的分层顶栏：导航与页签身份、缩放/视图控制、路径面包屑、筛选工具带和结果状态分层，不再把控件堆成无层次的连续表单。
- [ ] 保留现有 `VirtualMasonryGrid`、`AssetCard`、`FileBrowserMultiSelect` 和查询仓储；外观重排不得改变 `{rootID, path}`、空筛选和颜色/RGB/HSL 请求契约。
- [ ] 顶栏交互必须有可访问名称、明确激活态和响应式窄宽布局；颜色、标签、扩展名和排序入口仍然是真实控件，不使用静态装饰按钮替代。
- [ ] 验收：聚焦挂载测试覆盖导航、缩放、属性显示和筛选表单；`pnpm run dev:once` 通过；桌面真实截图和大结果滚动继续留在交互验收项，不由单元挂载测试代替。

## 2026-08-08 瀑布流容器填充与更新同步（进行中）

- [x] 画廊内容区和 `VirtualMasonryGrid` 根节点补齐 `flex`、`min-height`、`height` 和宽度契约，避免滚动容器测量为零时只渲染第一排。
- [x] 查询结果或卡片宽度变化后，在 DOM 提交阶段调用现有 `refreshLayout`，保留当前虚拟化设施和滚动锚点。
- [x] 卡片真实高度变化后重建 RBush 空间索引，使移动后的卡片在后续滚动中仍能进入可见范围。
- [ ] 仍需用真实桌面窗口确认容器高度、连续多排渲染和大结果滚动；挂载测试不能替代这项验收。

## 2026-08-08 已加载/总数状态与分页加载（进行中）

- [x] 顶部和底部状态改为显示真实的“已加载 / 总数”，不再把 `totalCount` 误当作当前已挂载卡片数。
- [x] 首批查询和后续分页共享当前筛选快照；筛选、目录切换或清空时重置分页游标、已加载集合和错误状态。
- [x] 复用 `VirtualMasonryGrid` 的滚动事件，在接近当前内容末尾时触发 `load-more`，追加下一页并按 `{rootID, path}` 去重。
- [x] 证据：`FileBrowserGalleryTab.interaction.test.ts` 已覆盖 `1/2 -> 2/2` 状态和第二页 `offset=200` 请求；fileBrowser 回归为 21 个文件、64 个用例，`pnpm run dev:once` 成功。
- [ ] 仍需真实桌面滚动到多页边界验收状态递增、末页停止和错误重试；挂载测试不能替代这项验收。

## 2026-08-08 扩展名筛选自动刷新（进行中）

- [x] 修正 `FileBrowserMultiSelect` 只更新控件值、未提交新查询的问题；扩展名变更现在按 SACAssetsManager 的行为以 300ms 防抖自动触发 `FileBrowserSearchPanel.submit`。
- [x] 手动提交、清空和组件销毁都会取消待执行的扩展名刷新，避免旧表单在目录切换或新查询上再次发出请求。
- [x] 统一走 `filequery.Service.Search` 的 `fileSearchNeedsMetadata=false` 文件系统枚举路径；`.png` 等扩展名在后端按规范化后缀过滤，不依赖当前首屏卡片做前端假过滤。
- [x] 证据：`FileBrowserSearchPanel.interaction.test.ts` 新增“只改扩展名自动发出请求”，`FileBrowserGalleryTab.interaction.test.ts` 验证目录范围携带 `exts`；两个文件共 13 个用例通过。
- [ ] 仍需真实桌面窗口确认筛选请求返回后旧卡片立即清空、结果只包含所选扩展名，以及快速连续勾选多个扩展名时只保留最后一次查询结果。

## 2026-08-09 视图模式选择器纠正（已完成切片）

- [x] 依据 `SACAssetsManager` 顶部工具栏原型，将文字下拉框恢复为紧凑的图标分段选择器；模式按钮直接对应 `masonry/grid/justified/list/table`，不把模式选择伪装成普通表单字段。
- [x] 每个模式按钮使用现有 `block__icon` 和图标系统，提供 `aria-label`、`title`、`aria-pressed` 当前态和原生按钮键盘操作；切换仍通过 `VirtualMasonryGrid` 的既有布局引擎和表格行投影完成。
- [x] 窄 Dock 的换行规则继续由 `@container file-gallery` 控制，模式组不会再受浏览器视口宽度误判而错位。
- [x] 证据：`pnpm exec vitest --run test/sforge/fileBrowser/FileBrowserGalleryTab.interaction.test.ts`（1 个文件、9 个用例）；`git diff --check` 通过。
- [x] `pnpm run dev:once` 全目标构建成功；构建后桌面页五个按钮的计算 `opacity` 均为 `1`，默认瀑布流为 `aria-pressed=true`。
- [x] A 级现场交互：点击“表格视图”后 `data-layout-mode=table` 且唯一激活按钮为表格，再点击“瀑布流视图”恢复 `data-layout-mode=masonry`。
- [ ] 各模式的大结果滚动和 TabRegistry 布局恢复仍登记在交互验收 TTT，不能由本次单页现场复核代替。

## 2026-08-09 文件加载器归并（进行中）

- [x] 对照 SACAssetsManager `94c8534a` 最终加载器注册序列，确认参考实现不止 SVG：还包括栅格图、D5M、SY 和 Windows 系统缩略图；历史 `pdf/eagle/onlyName` 不作为最终 Provider 依据。
- [x] 清理仓库根目录约 1.05 GB 的误留 `.tmp-gocache`，并固定忽略规则，避免 Go 编译缓存污染文件浏览树。
- [ ] 具体格式加载器和前端主预览/卡片策略迁移到独立 assets 子 TTT：[`文件加载器与预览策略.shorterm.ttt.md`](文件加载器与预览策略.shorterm.ttt.md)。

## 2026-08-10 画廊多选与拖放（本轮完成逻辑切片）

- [x] 画廊不再使用单独的 `selectedKey` 伪状态；卡片、表格行、树和属性 Dock 均读取共享 `{rootID,path}` 选择端口。
- [x] 卡片/表格行支持 Ctrl/Meta、Shift、方向键、Enter、Escape，并为当前结果提供框选矩形；框选只消费已渲染的真实 DOM 项，不猜测未加载结果。
- [x] 多选拖出复用 `FileBrowser.drag` 的受控 MIME 和结构守卫；目录页签接入单项移动与 `move-batch`，批量响应逐项失败可见，成功项清理选择后重新查询。
- [x] 选择端口新增地址序列测试；表格行新增键盘与多项拖放测试；画廊目录拖放目标测试新增，既有空筛选、分页和模式回归继续通过。
- [ ] SACAssetsManager 的原生文件系统拖出、标签页接收外部文件、虚拟化远端项目框选和桌面现场冲突态仍待后续切片；不以本轮挂载证据宣称完成。

## 2026-08-10 画廊互斥显示状态修正（本轮完成逻辑切片）

- [x] 画廊内容区统一由 `galleryDisplayState` 派生 `loading/error/empty/ready` 四种互斥状态；网格、表格列标题、框选层和空/错误提示不再各自维护独立条件链。
- [x] 修复“已有资源卡片同时显示此目录没有可展示的资源”：空提示仅在状态为 `empty` 时挂载，`ready` 状态只挂载真实 `VirtualMasonryGrid`。
- [x] 回归断言覆盖资源已加载时两种空提示均不存在；`pnpm exec vitest run --config vitest.config.ts test/sforge/fileBrowser/FileBrowserGalleryTab.interaction.test.ts --reporter=verbose`：1 个文件、10 个用例通过。
- [ ] 当前证据为 C 级真实 Vue 挂载；真实桌面窗口的状态切换、分页继续读取和错误重试仍需 A 级现场验收，不以本切片替代。
